// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Consumes messages via Nanomsg sockets (scalability protocols).
///
/// Currently only PULL and SUB sockets are supported.
///
/// Status: stable
module com.redpanda.benthos.inputs.Nanomsg
extends "../Input.pkl"

fixed plugin = "nanomsg"

/// A list of URLs to connect to (or as). If an item of the list contains commas it will
/// be expanded into multiple URLs.
urls: Listing<String>

/// Whether the URLs provided should be connected to, or bound as.
///
/// Default: `true`
bind: Boolean?

/// The socket type to use.
///
/// Default: `"PULL"`
socket_type: ("PULL"|"SUB")?

/// Whether messages that are rejected (nacked) at the output level should be
/// automatically replayed indefinitely, eventually resulting in back pressure if the
/// cause of the rejections is persistent. If set to `false` these messages will instead
/// be deleted. Disabling auto replays can greatly improve memory efficiency of high
/// throughput streams as the original shape of the data can be discarded immediately upon
/// consumption and mutation.
///
/// Default: `true`
auto_replay_nacks: Boolean?

/// A list of subscription topic filters to use when consuming from a SUB socket.
/// Specifying a single sub_filter of `''` will subscribe to everything.
sub_filters: Listing<String>?

/// The period to wait until a poll is abandoned and reattempted.
///
/// Default: `"5s"`
poll_timeout: String?
