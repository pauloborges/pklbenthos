// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Host endpoints (`/metrics` and `/stats`) for Prometheus scraping.
///
/// Status: stable
module com.redpanda.benthos.metrics.Prometheus
extends "../Metrics.pkl"

fixed plugin = "prometheus"

/// Whether to export timing metrics as a histogram, if `false` a summary is used instead.
/// When exporting histogram timings the delta values are converted from nanoseconds into
/// seconds in order to better fit within bucket definitions. For more information on
/// histograms and summaries refer to: https://prometheus.io/docs/practices/histograms/.
///
/// Default: `false`
use_histogram_timing: Boolean?

/// Timing metrics histogram buckets (in seconds). If left empty defaults to DefBuckets
/// (https://pkg.go.dev/github.com/prometheus/client_golang/prometheus#pkg-variables).
/// Applicable when `use_histogram_timing` is set to `true`.
histogram_buckets: Listing<Float>?

/// A list of timing metrics summary buckets (as quantiles). Applicable when
/// `use_histogram_timing` is set to `false`.
summary_quantiles_objectives: Listing<SummaryQuantilesObjectives>?

/// Whether to export process metrics such as CPU and memory usage in addition to Benthos
/// metrics.
///
/// Default: `false`
add_process_metrics: Boolean?

/// Whether to export Go runtime metrics such as GC pauses in addition to Benthos metrics.
///
/// Default: `false`
add_go_metrics: Boolean?

/// An optional [Push Gateway URL](#push-gateway) to push metrics to.
push_url: String?

/// The period of time between each push when sending metrics to a Push Gateway.
push_interval: String?

/// An identifier for push jobs.
///
/// Default: `"benthos_push"`
push_job_name: String?

/// The Basic Authentication credentials.
push_basic_auth: PushBasicAuth?

/// An optional file path to write all prometheus metrics on service shutdown.
file_output_path: String?

class SummaryQuantilesObjectives {
  /// Quantile value.
  ///
  /// Default: `0.0`
  quantile: Float?

  /// Permissible margin of error for quantile calculations. Precise calculations in a
  /// streaming context (without prior knowledge of the full dataset) can be
  /// resource-intensive. To balance accuracy with computational efficiency, an error
  /// margin is introduced. For instance, if the 90th quantile (`0.9`) is determined to be
  /// `100ms` with a 1% error margin (`0.01`), the true value will fall within the `[99ms,
  /// 101ms]` range.)
  ///
  /// Default: `0.0`
  error: Float?
}

class PushBasicAuth {
  /// The Basic Authentication username.
  username: String?

  /// The Basic Authentication password.
  ///
  /// This field holds a secret.
  password: String?
}
