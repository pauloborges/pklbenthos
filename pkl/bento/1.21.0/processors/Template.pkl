// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Transforms messages using Go template syntax.
///
/// Transforms messages using Go template syntax.
/// Supports built-in Go template functions,
/// Sprig template functions (including string manipulation, math, date, and encoding
/// utilities)
/// and a custom `meta` function to access message metadata (e.g., `{{ meta \"key\" }}`).
/// Additionally, users can define custom Bloblang-based functions via the `functions`
/// field, which are available during template execution.
///
/// Status: experimental
module com.warpstream.bento.processors.Template
extends "../Processor.pkl"

fixed plugin = "template"

/// The Go template to apply to messages. If `from_file` is enabled, this is used as the
/// file path and the template is loaded from the file.
text: String?

/// When enabled, the template is loaded from a file.
///
/// Default: `false`
from_file: Boolean?

/// A map of Bloblang functions to make available to the template.
functions: Mapping<String, String>?

/// A map of other templates which will defined into the `main` template.
sub_templates: Mapping<String, SubTemplates>?

class SubTemplates {
  /// The Go template. If `from_file` is enabled, this is used as the file path and the
  /// template is loaded from the file.
  text: String

  /// When enabled, the template is loaded from a file.
  ///
  /// Default: `false`
  from_file: Boolean?
}
