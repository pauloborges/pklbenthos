// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Describes how operational logs should be emitted.
module com.warpstream.bento.Logger

/// Set the minimum severity level for emitting logs.
///
/// Default: `"INFO"`
level: ("OFF"|"FATAL"|"ERROR"|"WARN"|"INFO"|"DEBUG"|"TRACE"|"ALL"|"NONE")?

/// Set the format of emitted logs.
///
/// Default: `"logfmt"`
format: ("json"|"logfmt")?

/// Whether to include timestamps in logs.
///
/// Default: `false`
add_timestamp: Boolean?

/// The name of the level field added to logs when the `format` is `json`.
///
/// Default: `"level"`
level_name: String?

/// The name of the timestamp field added to logs when `add_timestamp` is set to `true`
/// and the `format` is `json`.
///
/// Default: `"time"`
timestamp_name: String?

/// The name of the message field added to logs when the `format` is `json`.
///
/// Default: `"msg"`
message_name: String?

/// A map of key/value pairs to add to each structured log.
///
/// Default: `new { ["@service"] = "bento" }`
static_fields: Mapping<String, String>?

/// Experimental: Specify fields for optionally writing logs to a file.
file: File?

/// Experimental: When set, the logger will promote any log containing an `error` type to
/// the `ERROR` level.
///
/// Default: `false`
log_all_errors: Boolean?

class File {
  /// The file path to write logs to, if the file does not exist it will be created. Leave
  /// this field empty or unset to disable file based logging.
  path: String?

  /// Whether to rotate log files automatically.
  ///
  /// Default: `false`
  rotate: Boolean?

  /// The maximum number of days to retain old log files based on the timestamp encoded in
  /// their filename, after which they are deleted. Setting to zero disables this
  /// mechanism.
  ///
  /// Default: `0`
  rotate_max_age_days: Int?
}
