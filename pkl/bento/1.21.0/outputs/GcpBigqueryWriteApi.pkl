// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Sends messages as new rows to a Google Cloud BigQuery table using the BigQuery Storage
/// Write API.
///
/// You can use the Storage Write API to stream records into BigQuery in real time or to
/// batch process an arbitrarily large number of records and commit them in a single
/// atomic operation.
/// :::caution BigQuery API Limitation
/// The
/// [AppendRows](https://cloud.google.com/bigquery/docs/reference/storage/rpc/google.cloud.bigquery.storage.v1#appendrowsrequest)
/// request is limited to 10 MB.
///
/// If you experience issues with this limitation, tweak the component's batch policy
/// using the `batching` field. You can read more at [Message
/// Batching](https://warpstreamlabs.github.io/bento/docs/configuration/batching/#performance).
/// :::
///
/// Status: stable
module com.warpstream.bento.outputs.GcpBigqueryWriteApi
extends "../Output.pkl"

import "../Processor.pkl"

fixed plugin = "gcp_bigquery_write_api"

/// The project ID of the dataset to insert data to. If not set, it will be inferred from
/// the credentials or read from the GOOGLE_CLOUD_PROJECT environment variable.
project: String?

/// The BigQuery Dataset ID.
dataset: String

/// :::caution Interpolation of Message Batches
/// It is assumed that the first message in the batch will resolve the bloblang query and
/// that string will be used for all messages in the batch.
/// :::
/// The table to insert messages to.
table: String

/// Used to overwrite the default gRPC and HTTP BigQuery endpoints.
endpoint: Endpoint?

/// :::caution Storage API Stream Types
/// Only `DEFAULT` stream types are currently enabled. Future versions will see support
/// extended to `COMMITTED`, `BUFFERED`, and `PENDING`.
/// :::
/// sets the type of stream this write client is managing.
///
/// Default: `"DEFAULT"`
stream_type: "DEFAULT"?

/// Format of incoming messages
///
/// Default: `"json"`
message_format: ("json"|"protobuf")?

/// Allows you to configure a [batching policy](/docs/configuration/batching).
batching: Batching?

/// The maximum number of message batches to have in flight at a given time. Increase this
/// to improve throughput.
///
/// Default: `64`
max_in_flight: Int?

class Endpoint {
  /// The endpoint used to create the BigQuery client.
  http: String?

  /// The endpoint used to create the BigQuery Storage API client.
  grpc: String?
}

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A non-negative factor that adds random delay to batch flush intervals, where delay
  /// is determined uniformly at random between `0` and `jitter * period`. For example,
  /// with `period: 100ms` and `jitter: 0.1`, each flush will be delayed by a random
  /// duration between `0-10ms`.
  ///
  /// Default: `0.0`
  jitter: Float?

  /// A [Bloblang query](/docs/guides/bloblang/about/) that should return a boolean value
  /// indicating whether a message should end a batch.
  check: String?

  /// A list of [processors](/docs/components/processors/about) to apply to a batch as it
  /// is flushed. This allows you to aggregate and archive the batch however you see fit.
  /// Please note that all resulting messages are flushed as a single batch, therefore
  /// splitting the batch into smaller batches using these processors is a no-op.
  processors: Listing<Processor>?
}
