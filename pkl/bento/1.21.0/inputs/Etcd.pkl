// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Configures an etcd event watcher on a key or prefix.
///
/// Watches an etcd key or prefix for new events.
///
/// From the [etcd docs](https://etcd.io/docs/v3.5/learning/api/#watch-api):
/// > The Watch API provides an event-based interface for asynchronously monitoring
/// > changes to keys.
/// > An etcd watch waits for changes to keys by continuously watching from a given
/// > revision,
/// > either current or historical, and streams key updates back to the client.
/// >
/// > Watches are long-running requests and use gRPC streams to stream event data.
/// > A watch stream is bi-directional; the client writes to the stream to establish
/// > watches and reads to receive watch events.
/// > A single watch stream can multiplex many distinct watches by tagging events with
/// > per-watch identifiers.
/// > This multiplexing helps reducing the memory footprint and connection overhead on the
/// > core etcd cluster.
///
/// ### Events
///
/// Each event object is flattened and returned as an array, with each individual event in
/// the form:
///
/// ```json
/// {
///   "key": "<string or []byte>",
///   "value": "<string or []byte>",
///   "type": "<'PUT' or 'DELETE'>",
///   "version": "<int64>",
///   "mod_revision": "<int64>",
///   "create_revision": "<int64>",
///   "lease": "<int64>"
/// }
/// ```
///
/// Where a `key` or `value` is only a string if it is valid UTF-8.
///
/// Status: beta
module com.warpstream.bento.inputs.Etcd
extends "../Input.pkl"

fixed plugin = "etcd"

/// A set of URLs (schemes, hosts and ports only) that can be used to communicate with a
/// logical etcd cluster. If multiple endpoints are provided, the Client will attempt to
/// use them all in the event that one or more of them are unusable.
endpoints: Listing<String>

/// Optional configuration of etcd authentication headers.
auth: Auth?

/// Timeout for failing to establish a connection.
///
/// Default: `"5s"`
dial_timeout: String?

/// Time after which client pings the server to see if transport is alive.
///
/// Default: `"5s"`
keep_alive_time: String?

/// Time that the client waits for a response for the keep-alive probe. If the response is
/// not received in this time, the connection is closed.
///
/// Default: `"1s"`
keep_alive_timeout: String?

/// Timeout for a single request. This includes connection time, any redirects, and header
/// wait time.
///
/// Default: `"1s"`
request_timeout: String?

/// Custom TLS settings can be used to override system defaults.
tls: Tls?

/// The interval to update endpoints with its latest members. 0 disables auto-sync. By
/// default auto-sync is disabled.
auto_sync_interval: String?

/// The client-side request send limit in bytes. If 0, it defaults to 2.0 MiB (2 * 1024 *
/// 1024).
max_call_send_msg_size: Int?

/// The client-side response receive limit. If 0, it defaults to math.MaxInt32.
max_call_recv_msg_size: Int?

/// When set, will refuse to create a client against an outdated cluster.
///
/// Default: `false`
reject_old_cluster: Boolean?

/// When set, will allow client to send keepalive pings to server without any active
/// streams (RPCs).
///
/// Default: `false`
permit_without_stream: Boolean?

/// The maximum number of retries for unary RPCs.
max_unary_retries: Int?

/// The wait time before retrying an RPC.
backoff_wait_between: String?

/// The jitter fraction to randomize backoff wait time.
backoff_jitter_fraction: Float?

/// The key or prefix being watched. For prefix watching, options.with_prefix should be
/// `true`
key: String

/// Collection of options to configure an etcd watcher.
options: Options?

/// Whether messages that are rejected (nacked) at the output level should be
/// automatically replayed indefinitely, eventually resulting in back pressure if the
/// cause of the rejections is persistent. If set to `false` these messages will instead
/// be deleted. Disabling auto replays can greatly improve memory efficiency of high
/// throughput streams as the original shape of the data can be discarded immediately upon
/// consumption and mutation.
///
/// Default: `true`
auto_replay_nacks: Boolean?

class Auth {
  /// Whether to use password authentication
  ///
  /// Default: `false`
  enabled: Boolean?

  /// The username to authenticate as.
  username: String?

  /// The password to authenticate with.
  ///
  /// This field holds a secret.
  password: String?
}

class Tls {
  /// Whether custom TLS settings are enabled.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// Whether to skip server side certificate verification.
  ///
  /// Default: `false`
  skip_cert_verify: Boolean?

  /// Whether to allow the remote server to repeatedly request renegotiation. Enable this
  /// option if you're seeing the error message `local error: tls: no renegotiation`.
  ///
  /// Default: `false`
  enable_renegotiation: Boolean?

  /// An optional root certificate authority to use. This is a string, representing a
  /// certificate chain from the parent trusted root certificate, to possible intermediate
  /// signing certificates, to the host certificate.
  ///
  /// This field holds a secret.
  root_cas: String?

  /// An optional path of a root certificate authority file to use. This is a file, often
  /// with a .pem extension, containing a certificate chain from the parent trusted root
  /// certificate, to possible intermediate signing certificates, to the host certificate.
  root_cas_file: String?

  /// A list of client certificates to use. For each certificate either the fields `cert`
  /// and `key`, or `cert_file` and `key_file` should be specified, but not both.
  client_certs: Listing<TlsClientCerts>?
}

class TlsClientCerts {
  /// A plain text certificate to use.
  cert: String?

  /// A plain text certificate key to use.
  ///
  /// This field holds a secret.
  key: String?

  /// The path of a certificate to use.
  cert_file: String?

  /// The path of a certificate key to use.
  key_file: String?

  /// A plain text password for when the private key is password encrypted in PKCS#1 or
  /// PKCS#8 format. The obsolete `pbeWithMD5AndDES-CBC` algorithm is not supported for
  /// the PKCS#8 format. Warning: Since it does not authenticate the ciphertext, it is
  /// vulnerable to padding oracle attacks that can let an attacker recover the plaintext.
  ///
  /// This field holds a secret.
  password: String?
}

class Options {
  /// Whether to watch for events on a prefix.
  ///
  /// Default: `false`
  with_prefix: Boolean?

  /// Whether to send periodic progress updates every 10 minutes when there is no incoming
  /// events.
  ///
  /// Default: `false`
  with_progress_notify: Boolean?

  /// Whether to discard PUT events from the watcher.
  ///
  /// Default: `false`
  with_put_filter: Boolean?

  /// Whether to discard DELETE events from the watcher.
  ///
  /// Default: `false`
  with_delete_filter: Boolean?

  /// Whether to send CREATED notify events to the watcher.
  ///
  /// Default: `false`
  with_created_notify: Boolean?

  /// Will cause the watcher to return a range of lexicographically sorted keys to return
  /// in the form `[key, end)` where `end` is the passed parameter.
  with_range: String?
}
