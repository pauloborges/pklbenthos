// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Reads messages from an Apache Pulsar server.
///
/// ### Metadata
///
/// This input adds the following metadata fields to each message:
///
/// ```text
/// - pulsar_message_id
/// - pulsar_key
/// - pulsar_ordering_key
/// - pulsar_event_time_unix
/// - pulsar_publish_time_unix
/// - pulsar_topic
/// - pulsar_producer_name
/// - pulsar_redelivery_count
/// - All properties of the message
/// ```
///
/// You can access these metadata fields using
/// [function interpolation](/docs/configuration/interpolation#bloblang-queries).
///
/// Status: stable
module com.warpstream.bento.inputs.Pulsar
extends "../Input.pkl"

fixed plugin = "pulsar"

/// A URL to connect to.
url: String

/// A list of topics to subscribe to. This or topics_pattern must be set.
topics: Listing<String>?

/// A regular expression matching the topics to subscribe to. This or topics must be set.
topics_pattern: String?

/// Specify the subscription name for this consumer.
subscription_name: String

/// Specify the subscription type for this consumer.
///
/// > NOTE: Using a `key_shared` subscription type will __allow out-of-order delivery__
/// > since nack-ing messages sets non-zero nack delivery delay - this can potentially
/// > cause consumers to stall. See [Pulsar
/// > documentation](https://pulsar.apache.org/docs/en/2.8.1/concepts-messaging/#negative-acknowledgement)
/// > and [this Github issue](https://github.com/apache/pulsar/issues/12208) for more
/// > details.
///
/// Default: `"shared"`
subscription_type: ("shared"|"key_shared"|"failover"|"exclusive")?

/// Specify the path to a custom CA certificate to trust broker TLS service.
tls: Tls?

/// Optional configuration of Pulsar authentication methods.
auth: Auth?

class Tls {
  /// An optional path of a root certificate authority file to use. This is a file, often
  /// with a .pem extension, containing a certificate chain from the parent trusted root
  /// certificate, to possible intermediate signing certificates, to the host certificate.
  root_cas_file: String?
}

class Auth {
  /// Parameters for Pulsar OAuth2 authentication.
  oauth2: AuthOauth2?

  /// Parameters for Pulsar Token authentication.
  token: AuthToken?
}

class AuthOauth2 {
  /// Whether OAuth2 is enabled.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// OAuth2 audience.
  audience: String?

  /// OAuth2 issuer URL.
  issuer_url: String?

  /// The path to a file containing a private key.
  private_key_file: String?
}

class AuthToken {
  /// Whether Token Auth is enabled.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// Actual base64 encoded token.
  token: String?
}
