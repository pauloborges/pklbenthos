// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Executes a provided JavaScript code block or file for each message.
///
/// The [execution engine](https://github.com/dop251/goja) behind this processor provides
/// full ECMAScript 5.1 support (including regex and strict mode). Most of the ECMAScript
/// 6 spec is implemented but this is a work in progress.
///
/// Imports via `require` should work similarly to NodeJS, and access to the console is
/// supported which will print via the Bento logger. More caveats can be [found
/// here](https://github.com/dop251/goja#known-incompatibilities-and-caveats).
///
/// This processor is implemented using the
/// [github.com/dop251/goja](https://github.com/dop251/goja) library.
///
/// Status: experimental
module com.warpstream.bento.processors.Javascript
extends "../Processor.pkl"

fixed plugin = "javascript"

/// An inline JavaScript program to run. One of `code` or `file` must be defined.
code: String?

/// A file containing a JavaScript program to run. One of `code` or `file` must be
/// defined.
file: String?

/// List of folders that will be used to load modules from if the requested JS module is
/// not found elsewhere.
global_folders: Listing<String>?
