// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Sends log messages to the Datadog Logs API.
///
/// Submits log entries to Datadog using the HTTP Logs intake API.
///
/// ### Limits
///
/// - Maximum payload size (uncompressed): 5 MB
/// - Maximum size for a single log: 1 MB
/// - Maximum number of logs per batch: 1,000 entries
///
/// Logs exceeding 1 MB are truncated by Datadog but still accepted (2xx). Payloads
/// exceeding 5 MB are rejected with a 413.
///
/// :::warning
/// Log events with a timestamp older than 18 hours in the past will be rejected.
/// :::
///
/// ### Authentication
///
/// Set `api_key` explicitly or via the `DD_API_KEY` environment variable.
///
/// Status: beta
module com.warpstream.bento.outputs.DatadogLogs
extends "../Output.pkl"

import "../Processor.pkl"

fixed plugin = "datadog_logs"

/// The Datadog API key. If unset, falls back to the `DD_API_KEY` environment variable.
///
/// This field holds a secret.
api_key: String?

/// The Datadog site to send logs to. If unset, falls back to the `DD_SITE` environment
/// variable, then `datadoghq.com`.
site: String?

/// The source of the log, used for log processing rules.
source: String?

/// A comma-separated list of tags to attach to the log.
tags: String?

/// The hostname of the machine that produced the log.
hostname: String?

/// The name of the service that generated the log.
service: String?

/// The status of the log (e.g. info, warn, error).
status: String?

/// The timestamp of the log in epoch milliseconds. Defaults to the current time.
timestamp: String?

/// HTTP content encoding used to compress log payloads.
///
/// Default: `"gzip"`
content_encoding: ("gzip"|"identity"|"deflate")?

/// Override the API's destination endpoint with a custom host. Protocol scheme defaults
/// to 'http'.
endpoint: String?

/// Allows you to configure a [batching policy](/docs/configuration/batching).
batching: Batching?

/// The maximum number of messages to have in flight at a given time. Increase this to
/// improve throughput.
///
/// Default: `64`
max_in_flight: Int?

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A non-negative factor that adds random delay to batch flush intervals, where delay
  /// is determined uniformly at random between `0` and `jitter * period`. For example,
  /// with `period: 100ms` and `jitter: 0.1`, each flush will be delayed by a random
  /// duration between `0-10ms`.
  ///
  /// Default: `0.0`
  jitter: Float?

  /// A [Bloblang query](/docs/guides/bloblang/about/) that should return a boolean value
  /// indicating whether a message should end a batch.
  check: String?

  /// A list of [processors](/docs/components/processors/about) to apply to a batch as it
  /// is flushed. This allows you to aggregate and archive the batch however you see fit.
  /// Please note that all resulting messages are flushed as a single batch, therefore
  /// splitting the batch into smaller batches using these processors is a no-op.
  processors: Listing<Processor>?
}
