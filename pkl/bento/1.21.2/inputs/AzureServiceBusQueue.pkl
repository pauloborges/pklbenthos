// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Consume messages from an Azure Service Bus Queue.
///
/// Consume messages from an Azure Service Bus Queue using AMQP 1.0 protocol.
///
/// ### Authentication
///
/// Either `connection_string` or `namespace` must be provided. When using `namespace`,
/// the default Azure credentials will be used.
///
/// ### Metadata
///
/// This input adds the following metadata fields to each message:
///
/// ``` text
/// - service_bus_message_id
/// - service_bus_sequence_number
/// - service_bus_enqueued_time
/// - service_bus_delivery_count
/// - service_bus_locked_until
/// - service_bus_lock_token
/// - service_bus_expires_at
/// - service_bus_content_type
/// - service_bus_correlation_id
/// - service_bus_reply_to
/// - service_bus_reply_to_session_id
/// - service_bus_session_id
/// - service_bus_to
/// - service_bus_user_properties (flattened)
/// ```
///
/// You can access these metadata fields using [function
/// interpolation](/docs/configuration/interpolation#bloblang-queries).
///
/// Status: experimental
module com.warpstream.bento.inputs.AzureServiceBusQueue
extends "../Input.pkl"

fixed plugin = "azure_service_bus_queue"

/// The Service Bus connection string. This can be obtained from the Azure portal. If not
/// provided, namespace and default credentials will be used.
connection_string: String?

/// The Service Bus namespace. Required when connection_string is not provided.
namespace: String?

/// The name of the Service Bus queue to consume from.
queue: String

/// The maximum number of unprocessed messages to fetch at a given time.
///
/// Default: `10`
max_in_flight: Int?

/// Whether to automatically acknowledge messages as they are consumed rather than waiting
/// for acknowledgments from downstream. This can improve throughput but at the cost of
/// eliminating delivery guarantees.
///
/// Default: `false`
auto_ack: Boolean?

/// A list of regular expression patterns whereby if a message that has failed to be
/// delivered by Bento has an error that matches it will be nacked (or sent to dead letter
/// queue if configured). By default failed messages are nacked with requeue enabled.
nack_reject_patterns: Listing<String>?

/// Automatically renew message locks to prevent lock expiration during processing. Useful
/// for long-running message processing.
///
/// Default: `true`
renew_lock: Boolean?
