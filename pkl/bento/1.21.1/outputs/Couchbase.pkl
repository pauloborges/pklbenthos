// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Performs operations against Couchbase for each message, allowing you to store data
/// within message payloads.
///
/// When inserting, replacing or upserting documents, each must have the `content`
/// property set.
///
/// ### Concurrent Document Mutations
/// To prevent read/write conflicts, Couchbase returns a [_Compare And Swap_
/// (CAS)](https://docs.couchbase.com/go-sdk/current/howtos/concurrent-document-mutations.html)
/// value with each accessed document. Bento stores these as key/value pairs in metadata
/// with the `couchbase_cas` field. Note: CAS checks are enabled by default. You can
/// configure this by changing the value of `cas_enabled: false`.
///
/// Status: experimental
module com.warpstream.bento.outputs.Couchbase
extends "../Output.pkl"

import "../Processor.pkl"

fixed plugin = "couchbase"

/// Couchbase connection string.
url: String

/// Username to connect to the cluster.
username: String?

/// Password to connect to the cluster.
///
/// This field holds a secret.
password: String?

/// Couchbase bucket.
bucket: String

/// Bucket collection.
///
/// Default: `"_default"`
collection: String?

/// Couchbase transcoder to use.
///
/// Default: `"legacy"`
transcoder: ("json"|"legacy"|"raw"|"rawjson"|"rawstring")?

/// Operation timeout.
///
/// Default: `"15s"`
timeout: String?

/// Document id.
id: String

/// Document content.
content: String?

/// An optional TTL to set for items.
ttl: String?

/// Enable CAS validation.
///
/// Default: `true`
cas_enabled: Boolean?

/// Couchbase operation to perform.
///
/// Default: `"upsert"`
operation: ("decrement"|"increment"|"insert"|"remove"|"replace"|"upsert")?

/// The maximum number of messages to have in flight at a given time. Increase this to
/// improve throughput.
///
/// Default: `64`
max_in_flight: Int?

/// Allows you to configure a [batching policy](/docs/configuration/batching).
batching: Batching?

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A non-negative factor that adds random delay to batch flush intervals, where delay
  /// is determined uniformly at random between `0` and `jitter * period`. For example,
  /// with `period: 100ms` and `jitter: 0.1`, each flush will be delayed by a random
  /// duration between `0-10ms`.
  ///
  /// Default: `0.0`
  jitter: Float?

  /// A [Bloblang query](/docs/guides/bloblang/about/) that should return a boolean value
  /// indicating whether a message should end a batch.
  check: String?

  /// A list of [processors](/docs/components/processors/about) to apply to a batch as it
  /// is flushed. This allows you to aggregate and archive the batch however you see fit.
  /// Please note that all resulting messages are flushed as a single batch, therefore
  /// splitting the batch into smaller batches using these processors is a no-op.
  processors: Listing<Processor>?
}
