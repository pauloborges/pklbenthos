// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Decodes [Parquet files](https://parquet.apache.org/docs/) into a batch of structured
/// messages.
///
/// This processor uses
/// [https://github.com/parquet-go/parquet-go](https://github.com/parquet-go/parquet-go),
/// which is itself experimental. Therefore changes could be made into how this processor
/// functions outside of major version releases.
///
/// Status: experimental
module com.warpstream.bento.processors.ParquetDecode
extends "../Processor.pkl"

fixed plugin = "parquet_decode"

/// Whether to extract BYTE_ARRAY and FIXED_LEN_BYTE_ARRAY values as strings rather than
/// byte slices in all cases. Values with a logical type of UTF8 will automatically be
/// extracted as strings irrespective of this field. Enabling this field makes serialising
/// the data as JSON more intuitive as `[]byte` values are serialised as base64 encoded
/// strings by default.
///
/// Deprecated.
///
/// Default: `false`
byte_array_as_string: Boolean?

/// Whether to decode`LIST`type columns into their Parquet logical type format `{"list":
/// [{"element": value_1}, {"element": value_2}, ...]}` instead of a Go slice `[value_1,
/// value_2, ...]`.
///
/// :::caution
/// This flag will be disabled (set to `false`) by default and deprecated in future
/// versions, with the logical format being deprecated in favour of the Go slice.
/// :::
///
/// Default: `true`
use_parquet_list_format: Boolean?
