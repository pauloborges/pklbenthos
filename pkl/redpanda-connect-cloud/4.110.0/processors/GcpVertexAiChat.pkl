// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Generates responses to messages in a chat conversation, using the Vertex AI API.
///
/// This processor sends prompts to your chosen large language model (LLM) and generates
/// text from the responses, using the Vertex AI API.
///
/// For more information, see the [Vertex AI
/// documentation](https://cloud.google.com/vertex-ai/docs).
///
/// Status: stable
module com.redpanda.connect.cloud.processors.GcpVertexAiChat
extends "../Processor.pkl"

import "../Processor.pkl"

fixed plugin = "gcp_vertex_ai_chat"

/// GCP project ID to use
project: String

/// An optional field to set google Service Account Credentials json.
///
/// This field holds a secret.
credentials_json: String?

/// The location of the model if using a fined tune model. For base models this can be
/// omitted
location: String

/// The name of the LLM to use. For a full list of models, see the [Vertex AI Model
/// Garden](https://console.cloud.google.com/vertex-ai/model-garden).
model: String

/// The prompt you want to generate a response for. By default, the processor submits the
/// entire payload as a string.
prompt: String?

/// The system prompt to submit to the Vertex AI LLM.
system_prompt: String?

/// Historical messages to include in the chat request. The result of the bloblang query
/// should be an array of objects of the form of [{"role": "", "content":""}], where role
/// is "user" or "model".
history: String?

/// Additional data like an image to send with the prompt to the model. The result of the
/// mapping must be a byte array, and the content type is automatically detected.
attachment: String?

/// Controls the randomness of predications.
temperature: Float?

/// The maximum number of output tokens to generate per message.
max_tokens: Int?

/// The response format of generated type, the model must also be prompted to output the
/// appropriate response type.
///
/// Default: `"text"`
response_format: ("text"|"json")?

/// If specified, nucleus sampling will be used.
top_p: Float?

/// If specified top-k sampling will be used.
top_k: Float?

/// Stop sequences to when the model will stop generating further tokens.
stop: Listing<String>?

/// Positive values penalize new tokens based on whether they appear in the text so far,
/// increasing the model's likelihood to talk about new topics.
presence_penalty: Float?

/// Positive values penalize new tokens based on their existing frequency in the text so
/// far, decreasing the model's likelihood to repeat the same line verbatim.
frequency_penalty: Float?

/// The maximum number of sequential tool calls.
///
/// Default: `10`
max_tool_calls: Int?

/// The tools to allow the LLM to invoke. This allows building subpipelines that the LLM
/// can choose to invoke to execute agentic-like actions.
tools: Listing<Tools>?

class Tools {
  /// The name of this tool.
  name: String

  /// A description of this tool, the LLM uses this to decide if the tool should be used.
  description: String

  /// The parameters the LLM needs to provide to invoke this tool.
  parameters: ToolsParameters?

  /// The pipeline to execute when the LLM uses this tool.
  processors: Listing<Processor>?
}

class ToolsParameters {
  /// The required parameters for this pipeline.
  required: Listing<String>?

  /// The properties for the processor's input data
  properties: Mapping<String, ToolsParametersProperties>?
}

class ToolsParametersProperties {
  /// The type of this parameter.
  type: String

  /// A description of this parameter.
  description: String

  /// Specifies that this parameter is an enum and only these specific values should be
  /// used.
  enum: Listing<String>?
}
