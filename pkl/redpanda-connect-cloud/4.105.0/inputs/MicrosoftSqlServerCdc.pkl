// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Enables Change Data Capture by consuming from Microsoft SQL Server's change tables.
///
/// Streams changes from a Microsoft SQL Server database for Change Data Capture (CDC).
/// Additionally, if `stream_snapshot` is set to true, then the existing data in the
/// database is also streamed too.
///
/// ## Metadata
///
/// This input adds the following metadata fields to each message:
/// - database_schema (The database schema for the table where the message originates
///   from)
/// - schema (The table schema in benthos common schema format, compatible with processors
///   like parquet_encode)
/// - table (Name of the table that the message originated from)
/// - operation (Type of operation that generated the message: "read", "delete", "insert",
///   or "update_before" and "update_after". "read" is from messages that are read in the
///   initial snapshot phase.)
/// - lsn (the Log Sequence Number in Microsoft SQL Server)
///
/// ## Permissions
///
/// When using the default Microsoft SQL Server based cache, the Connect user requires
/// permission to create tables and stored procedures, and the rpcn schema must already
/// exist. Refer to `checkpoint_cache_table_name` for more information.
///
/// Status: stable
module com.redpanda.connect.cloud.inputs.MicrosoftSqlServerCdc
extends "../Input.pkl"

import "../Processor.pkl"

fixed plugin = "microsoft_sql_server_cdc"

/// The connection string of the Microsoft SQL Server database to connect to.
connection_string: String

/// If set to true, the connector will query all the existing data as a part of snapshot
/// process. Otherwise, it will start from the current Log Sequence Number position.
///
/// Default: `false`
stream_snapshot: Boolean?

/// Specifies a number of tables that will be processed in parallel during the snapshot
/// processing stage.
///
/// Default: `1`
max_parallel_snapshot_tables: Int?

/// The maximum number of rows to be streamed in a single batch when taking a snapshot.
///
/// Default: `1000`
snapshot_max_batch_size: Int?

/// Regular expressions for tables to include.
include: Listing<String>

/// Regular expressions for tables to exclude.
exclude: Listing<String>?

/// A [cache
/// resource](https://www.docs.redpanda.com/redpanda-connect/components/caches/about) to
/// use for storing the current Log Sequence Number (LSN) that has been successfully
/// delivered, this allows Redpanda Connect to continue from that Log Sequence Number
/// (LSN) upon restart, rather than consume the entire state of the change table. If not
/// set the default Microsoft SQL Server based cache will be used, see
/// `checkpoint_cache_table_name` for more information.
checkpoint_cache: String?

/// The multipart identifier for the checkpoint cache table name. If no `checkpoint_cache`
/// field is specified, this input will automatically create a table and stored procedure
/// under the `rpcn` schema to act as a checkpoint cache. This table stores the latest
/// processed Log Sequence Number (LSN) that has been successfully delivered, allowing
/// Redpanda Connect to resume from that point upon restart rather than reconsume the
/// entire change table.
///
/// Default: `"rpcn.CdcCheckpointCache"`
checkpoint_cache_table_name: String?

/// An optional connection string for a remote Microsoft SQL Server to use for the
/// checkpoint cache. When set, the checkpoint cache table is created on this remote
/// server instead of the source database. If `checkpoint_cache` is also set, that takes
/// precedence.
checkpoint_cache_connection_string: String?

/// The key to use to store the snapshot position in `checkpoint_cache`. An alternative
/// key can be provided if multiple CDC inputs share the same cache.
///
/// Default: `"microsoft_sql_server_cdc"`
checkpoint_cache_key: String?

/// The maximum number of messages that can be processed at a given time. Increasing this
/// limit enables parallel processing and batching at the output level. Any given Log
/// Sequence Number (LSN) will not be acknowledged unless all messages under that offset
/// are delivered in order to preserve at least once delivery guarantees.
///
/// Default: `1024`
checkpoint_limit: Int?

/// The interval between attempts to check for new changes once all data is processed. For
/// low traffic tables increasing this value can reduce network traffic to the server.
///
/// Default: `"5s"`
stream_backoff_interval: String?

/// Whether messages that are rejected (nacked) at the output level should be
/// automatically replayed indefinitely, eventually resulting in back pressure if the
/// cause of the rejections is persistent. If set to `false` these messages will instead
/// be deleted. Disabling auto replays can greatly improve memory efficiency of high
/// throughput streams as the original shape of the data can be discarded immediately upon
/// consumption and mutation.
///
/// Default: `true`
auto_replay_nacks: Boolean?

/// Allows you to configure a [batching
/// policy](https://docs.redpanda.com/redpanda-connect/configuration/batching).
batching: Batching?

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A [Bloblang query](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about)
  /// that should return a boolean value indicating whether a message should end a batch.
  check: String?

  /// A list of
  /// [processors](https://docs.redpanda.com/redpanda-connect/components/processors/about)
  /// to apply to a batch as it is flushed. This allows you to aggregate and archive the
  /// batch however you see fit. Please note that all resulting messages are flushed as a
  /// single batch, therefore splitting the batch into smaller batches using these
  /// processors is a no-op.
  processors: Listing<Processor>?
}
