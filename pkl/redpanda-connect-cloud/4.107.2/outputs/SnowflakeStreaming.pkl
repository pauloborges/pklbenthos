// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Ingest data into Snowflake using Snowpipe Streaming.
///
/// Ingest data into Snowflake using Snowpipe Streaming.
///
/// | Snowflake column type | Allowed format in Redpanda Connect |
/// | --- | --- |
/// | CHAR, VARCHAR | string |
/// | BINARY | []byte |
/// | NUMBER | any numeric type, string |
/// | FLOAT | any numeric type |
/// | BOOLEAN | bool,any numeric type,string parsable according to `strconv.ParseBool` |
/// | TIME,DATE,TIMESTAMP | unix or RFC 3339 with nanoseconds timestamps |
/// | VARIANT,ARRAY,OBJECT | any data type is converted into JSON |
/// | GEOGRAPHY,GEOMETRY | Not supported |
///
///
/// For TIMESTAMP, TIME and DATE columns, you can parse different string formats using a
/// bloblang `mapping`.
///
/// Authentication can be configured using a [RSA Key
/// Pair](https://docs.snowflake.com/en/user-guide/key-pair-auth).
///
/// There are
/// [limitations](https://docs.snowflake.com/en/user-guide/data-load-snowpipe-streaming-overview#limitations)
/// of what data types can be loaded into Snowflake using this method.
///
///
/// ## Performance
///
/// This output benefits from sending multiple messages in flight in parallel for improved
/// performance. You can tune the max number of in flight messages (or message batches)
/// with the field `max_in_flight`.
///
/// This output benefits from sending messages as a batch for improved performance.
/// Batches can be formed at both the input and output level. You can find out more [in
/// this doc](https://docs.redpanda.com/redpanda-connect/configuration/batching).
///
/// It is recommended that each batches results in at least 16MiB of compressed output
/// being written to Snowflake.
/// You can monitor the output batch size using the
/// `snowflake_compressed_output_size_bytes` metric.
///
/// Status: stable
module com.redpanda.connect.cloud.outputs.SnowflakeStreaming
extends "../Output.pkl"

import "../Processor.pkl"

fixed plugin = "snowflake_streaming"

/// The Snowflake [Account
/// name](https://docs.snowflake.com/en/user-guide/admin-account-identifier.html#using-an-account-locator-as-an-identifier).
/// Which should be formatted as `<orgname>-<account_name>` where `<orgname>` is the name
/// of your Snowflake organization and `<account_name>` is the unique name of your account
/// within your organization.
account: String

/// Override the default URL used to connect to Snowflake which is
/// https://ORG-ACCOUNT.snowflakecomputing.com
url: String?

/// The user to run the Snowpipe Stream as. See [Snowflake
/// Documentation](https://docs.snowflake.com/en/user-guide/admin-user-management) on how
/// to create a user.
user: String

/// The role for the `user` field. The role must have the [required
/// privileges](https://docs.snowflake.com/en/user-guide/data-load-snowpipe-streaming-overview#required-access-privileges)
/// to call the Snowpipe Streaming APIs. See [Snowflake
/// Documentation](https://docs.snowflake.com/en/user-guide/admin-user-management#user-roles)
/// for more information about roles.
role: String

/// The Snowflake database to ingest data into.
database: String

/// The Snowflake schema to ingest data into.
schema: String

/// The Snowflake table to ingest data into.
table: String

/// The PEM encoded private RSA key to use for authenticating with Snowflake. Either this
/// or `private_key_file` must be specified.
///
/// This field holds a secret.
private_key: String?

/// The file to load the private RSA key from. This should be a `.p8` PEM encoded file.
/// Either this or `private_key` must be specified.
private_key_file: String?

/// The RSA key passphrase if the RSA key is encrypted.
///
/// This field holds a secret.
private_key_pass: String?

/// A bloblang mapping to execute on each message.
mapping: String?

/// Optional SQL statements to execute immediately upon the first connection. This is a
/// useful way to initialize tables before processing data. Care should be taken to ensure
/// that the statement is idempotent, and therefore would not cause issues when run
/// multiple times after service restarts.
init_statement: String?

/// Options to control schema evolution within the pipeline as new columns are added to
/// the pipeline.
schema_evolution: SchemaEvolution?

/// The maximum amount of parallelism to use when building the output for Snowflake. The
/// metric to watch to see if you need to change this is
/// `snowflake_build_output_latency_ns`.
///
/// Deprecated.
parallelism: Int?

/// Options to optimize the time to build output data that is sent to Snowflake. The
/// metric to watch to see if you need to change this is
/// `snowflake_build_output_latency_ns`.
build_options: BuildOptions?

/// Allows you to configure a [batching
/// policy](https://docs.redpanda.com/redpanda-connect/configuration/batching).
batching: Batching?

/// The maximum number of messages to have in flight at a given time. Increase this to
/// improve throughput.
///
/// Default: `4`
max_in_flight: Int?

/// The prefix to use when creating a channel name.
/// Duplicate channel names will result in errors and prevent multiple instances of
/// Redpanda Connect from writing at the same time.
/// By default if neither `channel_prefix` or `channel_name is specified then the output
/// will create a channel name that is based on the table FQN so there will only be a
/// single stream per table.
///
/// At most `max_in_flight` channels will be opened.
///
/// This option is mutually exclusive with `channel_name`.
///
/// > **Note:** There is a limit of 10,000 streams per table - if using more than 10k
/// > streams please reach out to Snowflake support.
channel_prefix: String?

/// The channel name to use.
/// Duplicate channel names will result in errors and prevent multiple instances of
/// Redpanda Connect from writing at the same time.
/// Note that batches are assumed to all contain messages for the same channel, so this
/// interpolation is only executed on the first
/// message in each batch. It's recommended to batch at the input level to ensure that
/// batches contain messages for the same channel
/// if using an input that is partitioned (such as an Apache Kafka topic).
///
/// This option is mutually exclusive with `channel_prefix`.
///
/// > **Note:** There is a limit of 10,000 streams per table - if using more than 10k
/// > streams please reach out to Snowflake support.
channel_name: String?

/// The offset token to use for exactly once delivery of data in the pipeline. When data
/// is sent on a channel, each message in a batch's offset token
/// is compared to the latest token for a channel. If the offset token is
/// lexicographically less than the latest in the channel, it's assumed the message is a
/// duplicate and
/// is dropped. This means it is *very important* to have ordered delivery to the output,
/// any out of order messages to the output will be seen as duplicates and dropped.
/// Specifically this means that retried messages could be seen as duplicates if later
/// messages have succeeded in the meantime, so in most circumstances a dead letter queue
/// output should be employed for failed messages.
///
/// > **Note:** It's assumed that messages within a batch are in increasing order by
/// > offset token, additionally if you're using a numeric value as an offset token, make
/// > sure to pad
///       the value so that it's lexicographically ordered in its string representation, since offset tokens are compared in string form.
///
/// For more information about offset tokens, see [Snowflake
/// Documentation](https://docs.snowflake.com/en/user-guide/data-load-snowpipe-streaming-overview#offset-tokens)
offset_token: String?

/// Deprecated: use `commit_backoff.max_elapsed_time` instead.
///
/// Deprecated.
commit_timeout: String?

/// Control how frequently Snowflake is polled to check if data has been committed.
commit_backoff: CommitBackoff?

/// The format at which to expect incoming messages from the rest of the pipeline in.
///
/// Default: `"object"`
message_format: ("array"|"object")?

/// The format to parse string values for TIMESTAMP, TIMESTAMP_LTZ and TIMESTAMP_NTZ
/// columns. Should be a layout for [time.Parse](https://pkg.go.dev/time#Parse) in Golang.
///
/// Default: `"2006-01-02T15:04:05.999999999Z07:00"`
timestamp_format: String?

class SchemaEvolution {
  /// Whether schema evolution is enabled.
  enabled: Boolean

  /// If `true`, then new columns that are `null` are ignored and schema evolution is not
  /// triggered. If `false` then null columns trigger schema migrations in Snowflake.
  /// NOTE: unless you already know what type this column will be in advance, it's highly
  /// encouraged to ignore null values.
  ///
  /// Default: `true`
  ignore_nulls: Boolean?

  /// The mapping function from Redpanda Connect type to column type in Snowflake.
  /// Overriding this can allow for customization of the datatype if there is specific
  /// information that you know about the data types in use. This mapping should result in
  /// the `root` variable being assigned a string with the data type for the new column in
  /// Snowflake.
  ///
  ///         The input to this mapping is either the output of `processors` if specified, otherwise it is an object with the value and the name of the new column, the original message and table being written too. The metadata is unchanged from the original message that caused the schema to change. For example: `{"value": 42.3, "name":"new_data_field", "message": {"existing_data_field": 42, "new_data_field": "foo"}, "db": MY_DATABASE", "schema": "MY_SCHEMA", "table": "MY_TABLE"}
  ///
  /// Deprecated.
  new_column_type_mapping: String?

  /// A series of processors to execute when new columns are added to the table.
  /// Specifying this can support running side effects when the schema evolves or
  /// enriching the message with additional data to guide the schema changes. For example,
  /// one could read the schema the message was produced with from the schema registry and
  /// use that to decide which type the new column in Snowflake should be.
  ///
  ///         The input to these processors is an object with the value and the name of the new column, the original message and table being written too. The metadata is unchanged from the original message that caused the schema to change. For example: `{"value": 42.3, "name":"new_data_field", "message": {"existing_data_field": 42, "new_data_field": "foo"}, "db": MY_DATABASE", "schema": "MY_SCHEMA", "table": "MY_TABLE"}`. The output of these series of processors should be a single message, where the contents of the message is a string indicating the column data type to use (FLOAT, VARIANT, NUMBER(38, 0), etc. An ALTER TABLE statement will then be executed on the table in Snowflake to add the column with the corresponding data type.
  processors: Listing<Processor>?
}

class BuildOptions {
  /// The maximum amount of parallelism to use.
  ///
  /// Default: `1`
  parallelism: Int?

  /// The number of rows to chunk for parallelization.
  ///
  /// Default: `50000`
  chunk_size: Int?
}

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A [Bloblang query](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about)
  /// that should return a boolean value indicating whether a message should end a batch.
  check: String?

  /// A list of
  /// [processors](https://docs.redpanda.com/redpanda-connect/components/processors/about)
  /// to apply to a batch as it is flushed. This allows you to aggregate and archive the
  /// batch however you see fit. Please note that all resulting messages are flushed as a
  /// single batch, therefore splitting the batch into smaller batches using these
  /// processors is a no-op.
  processors: Listing<Processor>?
}

class CommitBackoff {
  /// The initial period to wait between status polls.
  ///
  /// Default: `"32ms"`
  initial_interval: String?

  /// The maximum period to wait between status polls.
  ///
  /// Default: `"512ms"`
  max_interval: String?

  /// The maximum total time to wait for data to be committed. If zero then no limit is
  /// used.
  ///
  /// Default: `"60s"`
  max_elapsed_time: String?

  /// The factor by which the poll interval grows on each attempt.
  ///
  /// Default: `2.0`
  multiplier: Float?
}
