// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Generates an image from a text description and other attributes, using OpenAI API.
///
/// This processor sends an image description and other attributes, such as image size and
/// quality to the OpenAI API, which generates an image. By default, the processor submits
/// the entire payload of each message as a string, unless you use the `prompt`
/// configuration field to customize it.
///
/// To learn more about image generation, see the [OpenAI API
/// documentation](https://platform.openai.com/docs/guides/images).
///
/// Status: stable
module com.redpanda.connect.cloud.processors.OpenaiImageGeneration
extends "../Processor.pkl"

fixed plugin = "openai_image_generation"

/// The Open API endpoint that the processor sends requests to. Update the default value
/// to use another OpenAI compatible service.
///
/// Default: `"https://api.openai.com/v1"`
server_address: String?

/// The API key for OpenAI API.
///
/// This field holds a secret.
api_key: String

/// The name of the OpenAI model to use.
model: String

/// A text description of the image you want to generate. The `prompt` field accepts a
/// maximum of 1000 characters for `dall-e-2` and 4000 characters for `dall-e-3`.
prompt: String?

/// The quality of the image to generate. Use `hd` to create images with finer details and
/// greater consistency across the image. This parameter is only supported for `dall-e-3`
/// models.
quality: String?

/// The size of the generated image. Choose from `256x256`, `512x512`, or `1024x1024` for
/// `dall-e-2`. Choose from `1024x1024`, `1792x1024`, or `1024x1792` for `dall-e-3`
/// models.
size: String?

/// The style of the generated image. Choose from `vivid` or `natural`. Vivid causes the
/// model to lean towards generating hyperreal and dramatic images. Natural causes the
/// model to produce more natural, less hyperreal looking images. This parameter is only
/// supported for `dall-e-3`.
style: String?
