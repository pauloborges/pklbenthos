// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Creates an input that consumes from a spanner change stream.
///
/// Consumes change records from a Google Cloud Spanner change stream. This input allows
/// you to track and process database changes in real-time, making it useful for data
/// replication, event-driven architectures, and maintaining derived data stores.
///
/// The input reads from a specified change stream within a Spanner database and converts
/// each change record into a message. The message payload contains the change records in
/// JSON format, and metadata is added with details about the Spanner instance, database,
/// and stream.
///
/// Change streams provide a way to track mutations to your Spanner database tables. For
/// more information about Spanner change streams, refer to the Google Cloud
/// documentation:
/// https://cloud.google.com/spanner/docs/change-streams
///
/// Status: stable
module com.redpanda.connect.cloud.inputs.GcpSpannerCdc
extends "../Input.pkl"

import "../Processor.pkl"

fixed plugin = "gcp_spanner_cdc"

/// Base64 encoded GCP service account JSON credentials file for authentication. If not
/// provided, Application Default Credentials (ADC) will be used.
credentials_json: String?

/// GCP project ID containing the Spanner instance
project_id: String

/// Spanner instance ID
instance_id: String

/// Spanner database ID
database_id: String

/// The name of the change stream to track, the stream must exist in the database. To
/// create a change stream, see
/// https://cloud.google.com/spanner/docs/change-streams/manage.
stream_id: String

/// RFC3339 formatted inclusive timestamp to start reading from the change stream
/// (default: current time)
start_timestamp: String?

/// RFC3339 formatted exclusive timestamp to stop reading at (default: no end time)
end_timestamp: String?

/// Duration string for heartbeat interval
///
/// Default: `"10s"`
heartbeat_interval: String?

/// The table to store metadata in (default: cdc_metadata_<stream_id>)
metadata_table: String?

/// Duration string for frequency of querying Spanner for minimum watermark.
///
/// Default: `"5s"`
min_watermark_cache_ttl: String?

/// List of modification types to process. If not specified, all modification types are
/// processed. Allowed values: INSERT, UPDATE, DELETE
allowed_mod_types: Listing<String>?

/// Allows you to configure a [batching
/// policy](https://docs.redpanda.com/redpanda-connect/configuration/batching).
batching: Batching?

/// The maximum number of messages that can be processed at a given time per partition.
/// Increasing this limit enables parallel processing and batching at the output level.
/// Any given partition watermark will not be committed unless all messages under that
/// offset are delivered in order to preserve at least once delivery guarantees.
///
/// Default: `1024`
checkpoint_limit: Int?

/// Whether messages that are rejected (nacked) at the output level should be
/// automatically replayed indefinitely, eventually resulting in back pressure if the
/// cause of the rejections is persistent. If set to `false` these messages will instead
/// be deleted. Disabling auto replays can greatly improve memory efficiency of high
/// throughput streams as the original shape of the data can be discarded immediately upon
/// consumption and mutation.
///
/// Default: `true`
auto_replay_nacks: Boolean?

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A [Bloblang query](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about)
  /// that should return a boolean value indicating whether a message should end a batch.
  check: String?

  /// A list of
  /// [processors](https://docs.redpanda.com/redpanda-connect/components/processors/about)
  /// to apply to a batch as it is flushed. This allows you to aggregate and archive the
  /// batch however you see fit. Please note that all resulting messages are flushed as a
  /// single batch, therefore splitting the batch into smaller batches using these
  /// processors is a no-op.
  processors: Listing<Processor>?
}
