// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Encodes [Parquet files](https://parquet.apache.org/docs/) from a batch of structured
/// messages.
///
/// This processor uses
/// [https://github.com/parquet-go/parquet-go](https://github.com/parquet-go/parquet-go),
/// which is itself experimental. Therefore changes could be made into how this processor
/// functions outside of major version releases.
///
/// Status: stable
module com.redpanda.connect.cloud.processors.ParquetEncode
extends "../Processor.pkl"

fixed plugin = "parquet_encode"

/// Parquet schema.
schema: Listing<Schema>?

/// Optionally specify a metadata field containing a schema definition to use for encoding
/// instead of a statically defined schema. For batches of messages, the first message's
/// schema will be applied to all subsequent messages of the batch.
schema_metadata: String?

/// The default compression type to use for fields.
///
/// Default: `"uncompressed"`
default_compression: ("uncompressed"|"snappy"|"gzip"|"brotli"|"zstd"|"lz4raw")?

/// The default encoding type to use for fields. A custom default encoding is only
/// necessary when consuming data with libraries that do not support
/// `DELTA_LENGTH_BYTE_ARRAY` and is therefore best left unset where possible.
///
/// Default: `"DELTA_LENGTH_BYTE_ARRAY"`
default_encoding: ("DELTA_LENGTH_BYTE_ARRAY"|"PLAIN")?

/// The precision used when encoding TIMESTAMP logical types. The default `NANOSECOND`
/// matches historical behaviour, but `TIMESTAMP(NANOS)` is not readable by Apache Spark
/// (Databricks), AWS Athena or DuckDB; set this to `MICROSECOND` (or `MILLISECOND`) when
/// writing Parquet files intended for consumption by those engines.
///
/// Default: `"NANOSECOND"`
default_timestamp_unit: ("NANOSECOND"|"MICROSECOND"|"MILLISECOND")?

class Schema {
  /// The name of the column.
  name: String

  /// The type of the column, only applicable for leaf columns with no child fields. Some
  /// logical types can be specified here such as UTF8.
  type: ("BOOLEAN"|"INT32"|"INT64"|"FLOAT"|"DOUBLE"|"BYTE_ARRAY"|"UTF8"|"TIMESTAMP"|"BSON"|"ENUM"|"JSON"|"UUID")?

  /// Whether the field is repeated.
  ///
  /// Default: `false`
  repeated: Boolean?

  /// Whether the field is optional.
  ///
  /// Default: `false`
  optional: Boolean?

  /// A list of child fields.
  fields: Listing<Any>?
}
