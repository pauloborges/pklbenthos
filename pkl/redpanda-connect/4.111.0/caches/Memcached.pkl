// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Connects to a cluster of memcached services, a prefix can be specified to allow
/// multiple cache types to share a memcached cluster under different namespaces.
///
/// Status: stable
module com.redpanda.connect.caches.Memcached
extends "../Cache.pkl"

fixed plugin = "memcached"

/// A list of addresses of memcached servers to use.
addresses: Listing<String>

/// An optional string to prefix item keys with in order to prevent collisions with
/// similar services.
prefix: String?

/// A default TTL to set for items, calculated from the moment the item is cached.
///
/// Default: `"300s"`
default_ttl: String?

/// Determine time intervals and cut offs for retry attempts.
retries: Retries?

class Retries {
  /// The initial period to wait between retry attempts.
  ///
  /// Default: `"1s"`
  initial_interval: String?

  /// The maximum period to wait between retry attempts
  ///
  /// Default: `"5s"`
  max_interval: String?

  /// The maximum overall period of time to spend on retry attempts before the request is
  /// aborted.
  ///
  /// Default: `"30s"`
  max_elapsed_time: String?
}
