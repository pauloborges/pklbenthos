// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Splits a [batch of
/// messages](https://docs.redpanda.com/redpanda-connect/configuration/batching) into N
/// batches, where each resulting batch contains a group of messages determined by a
/// [Bloblang query](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about).
///
/// Once the groups are established a list of processors are applied to their respective
/// grouped batch, which can be used to label the batch as per their grouping. Messages
/// that do not pass the check of any specified group are placed in their own group.
///
/// The functionality of this processor depends on being applied across messages that are
/// batched. You can find out more about batching [in this
/// doc](https://docs.redpanda.com/redpanda-connect/configuration/batching).
///
/// To further divide each group into individual messages, follow this processor with a
/// [`split`
/// processor](https://docs.redpanda.com/redpanda-connect/components/processors/split).
///
/// Status: stable
module com.redpanda.connect.processors.GroupBy
extends "../Processor.pkl"

import "../Processor.pkl"

fixed plugin = "group_by"

/// The value that this plugin takes.
///
/// Redpanda Connect reads a single value under the name of this plugin, not a set of
/// fields.
value: Listing<GroupBy>?

pluginValue = value

class GroupBy {
  /// A [Bloblang query](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about)
  /// that should return a boolean value indicating whether a message belongs to a given
  /// group.
  check: String

  /// A list of
  /// [processors](https://docs.redpanda.com/redpanda-connect/components/processors/about)
  /// to execute on the newly formed group.
  processors: Listing<Processor>?
}
