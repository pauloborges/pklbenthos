// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// A processor that applies a list of child processors to messages of a batch as though
/// they were each a batch of one message (similar to the
/// [`for_each`](https://docs.redpanda.com/redpanda-connect/components/processors/for_each)
/// processor), but where each message is processed in parallel.
///
/// The field `cap`, if greater than zero, caps the maximum number of parallel processing
/// threads.
///
/// The functionality of this processor depends on being applied across messages that are
/// batched. You can find out more about batching in
/// [batching](https://docs.redpanda.com/redpanda-connect/configuration/batching).
///
/// Status: stable
module com.redpanda.connect.processors.Parallel
extends "../Processor.pkl"

import "../Processor.pkl"

fixed plugin = "parallel"

/// The maximum number of messages to have processing at a given time.
///
/// Default: `0`
cap: Int?

/// A list of child processors to apply.
processors: Listing<Processor>
