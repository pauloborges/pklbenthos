// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Generates vector embeddings to represent input text, using the Cohere API.
///
/// This processor sends document strings to the Cohere API, which reranks them based on
/// the relevance to the query.
///
/// To learn more about reranking, see the [Cohere API
/// documentation](https://docs.cohere.com/docs/rerank-2).
///
/// The output of this processor is an array of objects, each containing a "document"
/// field with the original document content, a "relevance_score" field indicating how
/// relevant it is to the query, and an index field that refers to the document's position
/// within the input documents array. The objects are ordered by their relevance score
/// (highest first).
///
/// Status: stable
module com.redpanda.connect.processors.CohereRerank
extends "../Processor.pkl"

fixed plugin = "cohere_rerank"

/// The base URL to use for API requests.
///
/// Default: `"https://api.cohere.com"`
base_url: String?

/// The API key for the Cohere API.
///
/// This field holds a secret.
api_key: String

/// The name of the Cohere model to use.
model: String

/// The search query
query: String

/// A list of texts that will be compared to the query. For optimal performance Cohere
/// recommends against sending more than 1000 documents in a single request. NOTE:
/// structured data should be formatted as YAML for best performance.
documents: String

/// The number of documents to return, if 0 all documents are returned.
///
/// Default: `"0"`
top_n: String?

/// Long documents will be automatically truncated to the specified number of tokens.
///
/// Default: `4096`
max_tokens_per_doc: Int?
