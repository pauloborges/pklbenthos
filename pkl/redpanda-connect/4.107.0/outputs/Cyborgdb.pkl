// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Inserts items into a CyborgDB encrypted vector index.
///
/// This output allows you to write vectors to a CyborgDB encrypted index. CyborgDB
/// provides
/// end-to-end encrypted vector storage with automatic dimension detection and index
/// optimization.
///
/// All vector data is encrypted client-side before being sent to the server, ensuring
/// complete
/// data privacy. The encryption key never leaves your infrastructure.
///
/// Status: stable
module com.redpanda.connect.outputs.Cyborgdb
extends "../Output.pkl"

import "../Processor.pkl"

fixed plugin = "cyborgdb"

/// The maximum number of messages to have in flight at a given time. Increase this to
/// improve throughput.
///
/// Default: `64`
max_in_flight: Int?

/// Allows you to configure a [batching
/// policy](https://docs.redpanda.com/redpanda-connect/configuration/batching).
batching: Batching?

/// The host for the CyborgDB instance.
host: String

/// The CyborgDB API key for authentication.
///
/// This field holds a secret.
api_key: String

/// The name of the index to write to.
///
/// Default: `"redpanda-vectors"`
index_name: String?

/// The base64-encoded encryption key for the index. Must be exactly 32 bytes when
/// decoded.
///
/// This field holds a secret.
index_key: String

/// If true, create the index if it doesn't exist. CyborgDB will auto-detect dimension and
/// optimize the index.
///
/// Default: `false`
create_if_missing: Boolean?

/// The operation to perform against the CyborgDB index.
///
/// Default: `"upsert"`
operation: ("upsert"|"delete")?

/// The ID for the vector entry in CyborgDB.
id: String

/// The mapping to extract out the vector from the document. The result must be a floating
/// point array. Required for upsert operations.
vector_mapping: String?

/// An optional mapping of message to metadata for the vector entry.
metadata_mapping: String?

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A [Bloblang query](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about)
  /// that should return a boolean value indicating whether a message should end a batch.
  check: String?

  /// A list of
  /// [processors](https://docs.redpanda.com/redpanda-connect/components/processors/about)
  /// to apply to a batch as it is flushed. This allows you to aggregate and archive the
  /// batch however you see fit. Please note that all resulting messages are flushed as a
  /// single batch, therefore splitting the batch into smaller batches using these
  /// processors is a no-op.
  processors: Listing<Processor>?
}
