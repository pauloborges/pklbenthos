// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Writes batches of messages into Apache Doris using Stream Load.
///
/// Each output batch is encoded into a single Doris Stream Load request. The sink first
/// contacts FE and follows the Stream Load redirect to BE before uploading the batch
/// body. The batch is only acknowledged when Doris reports success.
///
/// ## Performance
///
/// This output benefits from sending multiple messages in flight in parallel for improved
/// performance. You can tune the max number of in flight messages (or message batches)
/// with the field `max_in_flight`.
///
/// This output benefits from sending messages as a batch for improved performance.
/// Batches can be formed at both the input and output level. You can find out more [in
/// this doc](https://docs.redpanda.com/redpanda-connect/configuration/batching).
///
/// Status: beta
module com.redpanda.connect.outputs.DorisStreamLoad
extends "../Output.pkl"

import "../Processor.pkl"

fixed plugin = "doris_stream_load"

/// Backward-compatible single Doris FE HTTP URL, for example http://fe_host:8030. When
/// fe_urls is provided it takes precedence.
url: String?

/// A list of Doris FE HTTP URLs. The sink will try these FE endpoints in order with
/// per-request failover, and the starting FE is rotated across requests.
fe_urls: Listing<String>?

/// Target Doris database.
database: String

/// Target Doris table.
table: String

/// Doris username.
username: String

/// Doris password.
///
/// This field holds a secret.
password: String

/// Doris FE MySQL query port, used by ConnectionTest to verify that the target database
/// and table exist. Set to 0 to disable query-port checks.
///
/// Default: `9030`
query_port: Int?

/// Body format sent to Doris Stream Load.
///
/// Default: `"json"`
format: ("json"|"csv")?

/// Encode a JSON batch as newline-delimited JSON and set the Doris header
/// read_json_by_line=true.
///
/// Default: `true`
read_json_by_line: Boolean?

/// Encode a JSON batch as a single JSON array and set the Doris header
/// strip_outer_array=true.
///
/// Default: `false`
strip_outer_array: Boolean?

/// Optional Doris jsonpaths header value.
jsonpaths: String?

/// Optional Doris json_root header value.
json_root: String?

/// Optional Doris columns header. When set the values are joined with commas.
columns: Listing<String>?

/// Optional Doris where header for filtering imported rows.
where: String?

/// Column separator used for csv format.
///
/// Default: `","`
column_separator: String?

/// Line delimiter used for csv batches.
///
/// Default: `"\n"`
line_delimiter: String?

/// Optional Doris group_commit mode. Valid values are sync_mode, async_mode, and
/// off_mode. When omitted Doris uses the server default behavior.
group_commit: String?

/// Optional Doris max_filter_ratio header value.
///
/// Default: `0.0`
max_filter_ratio: Float?

/// Optional Doris partitions header. Values are joined with commas.
partitions: Listing<String>?

/// Optional Doris temporary_partitions header. Values are joined with commas.
temporary_partitions: Listing<String>?

/// Optional Doris skip_lines header.
///
/// Default: `0`
skip_lines: Int?

/// Whether Doris should treat empty input fields as NULL.
///
/// Default: `false`
empty_field_as_null: Boolean?

/// Whether Doris should trim double quotes in CSV/text imports.
///
/// Default: `false`
trim_double_quotes: Boolean?

/// Whether Doris should treat JSON numbers as strings.
///
/// Default: `false`
num_as_string: Boolean?

/// Whether Doris should enable fuzzy_parse for JSON inputs.
///
/// Default: `false`
fuzzy_parse: Boolean?

/// Prefix used when generating Doris labels.
///
/// Default: `"redpanda_connect"`
label_prefix: String?

/// Optional additional static Stream Load headers. Reserved Doris headers configured by
/// this component take precedence.
headers: Mapping<String, String>?

/// Whether to set Doris strict_mode=true.
///
/// Default: `false`
strict_mode: Boolean?

/// Timeout for each Doris HTTP request.
///
/// Default: `"30s"`
timeout: String?

/// Maximum number of parallel in-flight Doris Stream Load requests.
///
/// Default: `8`
max_in_flight: Int?

/// Allows you to configure a [batching
/// policy](https://docs.redpanda.com/redpanda-connect/configuration/batching).
batching: Batching?

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A [Bloblang query](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about)
  /// that should return a boolean value indicating whether a message should end a batch.
  check: String?

  /// A list of
  /// [processors](https://docs.redpanda.com/redpanda-connect/components/processors/about)
  /// to apply to a batch as it is flushed. This allows you to aggregate and archive the
  /// batch however you see fit. Please note that all resulting messages are flushed as a
  /// single batch, therefore splitting the batch into smaller batches using these
  /// processors is a no-op.
  processors: Listing<Processor>?
}
