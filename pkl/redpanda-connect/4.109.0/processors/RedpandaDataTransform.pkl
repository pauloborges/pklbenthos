// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Executes a Redpanda Data Transform as a processor
///
/// This processor executes a Redpanda Data Transform WebAssembly module, calling
/// OnRecordWritten for each message being processed.
///
/// You can find out about how transforms work here:
/// [https://docs.redpanda.com/current/develop/data-transforms/how-transforms-work/](https://docs.redpanda.com/current/develop/data-transforms/how-transforms-work/)
///
/// Status: stable
module com.redpanda.connect.processors.RedpandaDataTransform
extends "../Processor.pkl"

fixed plugin = "redpanda_data_transform"

/// The path of the target WASM module to execute.
module_path: String

/// An optional key to populate for each message.
input_key: String?

/// An optional name of metadata for an output message key.
output_key: String?

/// Determine which (if any) metadata values should be added to messages as headers.
input_headers: InputHeaders?

/// Determine which (if any) message headers should be added to the output as metadata.
output_metadata: OutputMetadata?

/// An optional timestamp to set for each message. When left empty, the current timestamp
/// is used.
timestamp: String?

/// The maximum period of time for a message to be processed
///
/// Default: `"10s"`
timeout: String?

/// The maximum amount of wasm memory pages (64KiB) that an individual wasm module
/// instance can use
///
/// Default: `1600`
max_memory_pages: Int?

class InputHeaders {
  /// Provide a list of explicit metadata key prefixes to match against.
  include_prefixes: Listing<String>?

  /// Provide a list of explicit metadata key regular expression (re2) patterns to match
  /// against.
  include_patterns: Listing<String>?
}

class OutputMetadata {
  /// Provide a list of explicit metadata key prefixes to match against.
  include_prefixes: Listing<String>?

  /// Provide a list of explicit metadata key regular expression (re2) patterns to match
  /// against.
  include_patterns: Listing<String>?
}
