// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Cache the result of applying one or more processors to messages identified by a key.
/// If the key already exists within the cache the contents of the message will be
/// replaced with the cached result instead of applying the processors. This component is
/// therefore useful in situations where an expensive set of processors need only be
/// executed periodically.
///
/// The format of the data when stored within the cache is a custom and versioned schema
/// chosen to balance performance and storage space. It is therefore not possible to point
/// this processor to a cache that is pre-populated with data that this processor has not
/// created itself.
///
/// Status: stable
module com.redpanda.connect.processors.Cached
extends "../Processor.pkl"

import "../Processor.pkl"

fixed plugin = "cached"

/// The cache resource to read and write processor results from.
cache: String

/// A condition that can be used to skip caching the results from the processors.
skip_on: String?

/// A key to be resolved for each message, if the key already exists in the cache then the
/// cached result is used, otherwise the processors are applied and the result is cached
/// under this key. The key could be static and therefore apply generally to all messages
/// or it could be an interpolated expression that is potentially unique for each message.
key: String

/// An optional expiry period to set for each cache entry. Some caches only have a general
/// TTL and will therefore ignore this setting.
ttl: String?

/// The list of processors whose result will be cached.
processors: Listing<Processor>
