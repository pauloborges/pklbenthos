// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Consumes log events from AWS CloudWatch Logs.
///
/// Polls CloudWatch Log Groups for log events. Supports filtering by log streams,
/// CloudWatch filter patterns, and configurable start times.
///
/// Each log event becomes a separate message with metadata including the log group name,
/// log stream name, timestamp, and ingestion time.
///
/// > **Important:** This input tracks its position in memory only. If the process
/// > restarts, it will resume from the configured start_time (or the beginning if not
/// > set). For exactly-once processing, you should configure an appropriate start_time or
/// > implement idempotent downstream processing.
///
/// ## Credentials
///
/// By default Redpanda Connect will use a shared credentials file when connecting to AWS
/// services. It's also possible to set them explicitly at the component level, allowing
/// you to transfer data across accounts. You can find out more in
/// [cloud/aws](https://docs.redpanda.com/redpanda-connect/guides/cloud/aws).
///
/// ## Metadata
///
/// This input adds the following metadata fields to each message:
///
/// - `cloudwatch_log_group` - The name of the log group
/// - `cloudwatch_log_stream` - The name of the log stream
/// - `cloudwatch_timestamp` - The timestamp of the log event (Unix milliseconds)
/// - `cloudwatch_ingestion_time` - The ingestion timestamp (Unix milliseconds)
/// - `cloudwatch_event_id` - The unique event ID
///
/// You can access these metadata fields using
/// [Bloblang](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about).
///
/// Status: stable
module com.redpanda.connect.inputs.AwsCloudwatchLogs
extends "../Input.pkl"

fixed plugin = "aws_cloudwatch_logs"

/// The name of the CloudWatch Log Group to consume from.
log_group_name: String

/// An optional list of log stream names to consume from. If not set, events from all
/// streams in the log group will be consumed.
log_stream_names: Listing<String>?

/// An optional log stream name prefix to filter streams. Only streams starting with this
/// prefix will be consumed.
log_stream_prefix: String?

/// An optional CloudWatch Logs filter pattern to apply when querying log events. See AWS
/// documentation for filter pattern syntax.
filter_pattern: String?

/// The time to start consuming log events from. Can be an RFC3339 timestamp (e.g.,
/// `2024-01-01T00:00:00Z`) or the string `now` to start consuming from the current time.
/// If not set, starts from the beginning of available logs.
start_time: String?

/// The interval at which to poll for new log events.
///
/// Default: `"5s"`
poll_interval: String?

/// The maximum number of log events to return in a single API call. Valid range: 1-10000.
///
/// Default: `1000`
limit: Int?

/// Whether to output log events as structured JSON objects with all metadata fields, or
/// as plain text messages with metadata in message metadata.
///
/// Default: `true`
structured_log: Boolean?

/// The maximum time to wait for an API request to complete.
///
/// Default: `"30s"`
api_timeout: String?

/// Whether messages that are rejected (nacked) at the output level should be
/// automatically replayed indefinitely, eventually resulting in back pressure if the
/// cause of the rejections is persistent. If set to `false` these messages will instead
/// be deleted. Disabling auto replays can greatly improve memory efficiency of high
/// throughput streams as the original shape of the data can be discarded immediately upon
/// consumption and mutation.
///
/// Default: `true`
auto_replay_nacks: Boolean?

/// The AWS region to target.
region: String?

/// Allows you to specify a custom endpoint for the AWS API.
endpoint: String?

/// TCP socket configuration.
tcp: Tcp?

/// Optional manual configuration of AWS credentials to use. More information can be found
/// in [cloud/aws](https://docs.redpanda.com/redpanda-connect/guides/cloud/aws).
credentials: Credentials?

class Tcp {
  /// Maximum amount of time a dial will wait for a connect to complete. Zero disables.
  ///
  /// Default: `"0s"`
  connect_timeout: String?

  /// TCP keep-alive probe configuration.
  keep_alive: TcpKeepAlive?

  /// Maximum time to wait for acknowledgment of transmitted data before killing the
  /// connection. Linux-only (kernel 2.6.37+), ignored on other platforms. When enabled,
  /// keep_alive.idle must be greater than this value per RFC 5482. Zero disables.
  ///
  /// Default: `"0s"`
  tcp_user_timeout: String?
}

class TcpKeepAlive {
  /// Duration the connection must be idle before sending the first keep-alive probe. Zero
  /// defaults to 15s. Negative values disable keep-alive probes.
  ///
  /// Default: `"15s"`
  idle: String?

  /// Duration between keep-alive probes. Zero defaults to 15s.
  ///
  /// Default: `"15s"`
  interval: String?

  /// Maximum unanswered keep-alive probes before dropping the connection. Zero defaults
  /// to 9.
  ///
  /// Default: `9`
  count: Int?
}

class Credentials {
  /// A profile from `~/.aws/credentials` to use.
  profile: String?

  /// The ID of credentials to use.
  id: String?

  /// The secret for the credentials being used.
  ///
  /// This field holds a secret.
  secret: String?

  /// The token for the credentials being used, required when using short term
  /// credentials.
  token: String?

  /// Use the credentials of a host EC2 machine configured to assume [an IAM role
  /// associated with the
  /// instance](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use_switch-role-ec2.html).
  from_ec2_role: Boolean?

  /// A role ARN to assume.
  role: String?

  /// An external ID to provide when assuming a role.
  role_external_id: String?
}
