// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Send tracing events to an [Open Telemetry
/// collector](https://opentelemetry.io/docs/collector/).
///
/// Status: stable
module com.redpanda.connect.tracers.OpenTelemetryCollector
extends "../Tracer.pkl"

fixed plugin = "open_telemetry_collector"

/// The name of the service in traces.
///
/// Default: `"benthos"`
service: String?

/// A list of http collectors.
http: Listing<Http>?

/// A list of grpc collectors.
grpc: Listing<Grpc>?

/// A map of tags to add to all exported spans and metrics.
tags: Mapping<String, String>?

/// Settings for trace sampling. Sampling is recommended for high-volume production
/// workloads.
sampling: Sampling?

class Http {
  /// The endpoint of a collector to send events to.
  address: String?

  /// The URL of a collector to send events to.
  ///
  /// Deprecated.
  ///
  /// Default: `"localhost:4318"`
  url: String?

  /// Connect to the collector over HTTPS
  ///
  /// Default: `false`
  secure: Boolean?
}

class Grpc {
  /// The endpoint of a collector to send events to.
  address: String?

  /// The URL of a collector to send events to.
  ///
  /// Deprecated.
  ///
  /// Default: `"localhost:4317"`
  url: String?

  /// Connect to the collector with client transport security
  ///
  /// Default: `false`
  secure: Boolean?
}

class Sampling {
  /// Whether to enable sampling.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// Sets the ratio of traces to sample.
  ratio: Float?
}
