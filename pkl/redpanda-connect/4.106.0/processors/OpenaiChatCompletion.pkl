// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Generates responses to messages in a chat conversation, using the OpenAI API.
///
/// This processor sends the contents of user prompts to the OpenAI API, which generates
/// responses. By default, the processor submits the entire payload of each message as a
/// string, unless you use the `prompt` configuration field to customize it.
///
/// To learn more about chat completion, see the [OpenAI API
/// documentation](https://platform.openai.com/docs/guides/chat-completions).
///
/// Status: stable
module com.redpanda.connect.processors.OpenaiChatCompletion
extends "../Processor.pkl"

import "../Processor.pkl"

fixed plugin = "openai_chat_completion"

/// The Open API endpoint that the processor sends requests to. Update the default value
/// to use another OpenAI compatible service.
///
/// Default: `"https://api.openai.com/v1"`
server_address: String?

/// The API key for OpenAI API.
///
/// This field holds a secret.
api_key: String

/// The name of the OpenAI model to use.
model: String

/// The user prompt you want to generate a response for. By default, the processor submits
/// the entire payload as a string.
prompt: String?

/// The system prompt to submit along with the user prompt.
system_prompt: String?

/// The history of the prior conversation. A bloblang query that should result in an array
/// of objects of the form: [{"role": "user", "content": "<text>"}, {"role":"assistant",
/// "content":"<text>"}]
history: String?

/// An image to send along with the prompt. The mapping result must be a byte array.
image: String?

/// The maximum number of tokens that can be generated in the chat completion.
max_tokens: Int?

/// What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make
/// the output more random, while lower values like 0.2 will make it more focused and
/// deterministic.
///
/// We generally recommend altering this or top_p but not both.
temperature: Float?

/// A unique identifier representing your end-user, which can help OpenAI to monitor and
/// detect abuse.
user: String?

/// Specify the model's output format. If `json_schema` is specified, then additionally a
/// `json_schema` or `schema_registry` must be configured.
///
/// Default: `"text"`
response_format: ("text"|"json"|"json_schema")?

/// The JSON schema to use when responding in `json_schema` format. To learn more about
/// what JSON schema is supported see the [OpenAI
/// documentation](https://platform.openai.com/docs/guides/structured-outputs/supported-schemas).
json_schema: JsonSchema?

/// The schema registry to dynamically load schemas from when responding in `json_schema`
/// format. Schemas themselves must be in JSON format. To learn more about what JSON
/// schema is supported see the [OpenAI
/// documentation](https://platform.openai.com/docs/guides/structured-outputs/supported-schemas).
schema_registry: SchemaRegistry?

/// An alternative to sampling with temperature, called nucleus sampling, where the model
/// considers the results of the tokens with top_p probability mass. So 0.1 means only the
/// tokens comprising the top 10% probability mass are considered.
///
/// We generally recommend altering this or temperature but not both.
top_p: Float?

/// Number between -2.0 and 2.0. Positive values penalize new tokens based on their
/// existing frequency in the text so far, decreasing the model's likelihood to repeat the
/// same line verbatim.
frequency_penalty: Float?

/// Number between -2.0 and 2.0. Positive values penalize new tokens based on whether they
/// appear in the text so far, increasing the model's likelihood to talk about new topics.
presence_penalty: Float?

/// If specified, our system will make a best effort to sample deterministically, such
/// that repeated requests with the same seed and parameters should return the same
/// result. Determinism is not guaranteed.
seed: Int?

/// Up to 4 sequences where the API will stop generating further tokens.
stop: Listing<String>?

/// The tools to allow the LLM to invoke. This allows building subpipelines that the LLM
/// can choose to invoke to execute agentic-like actions.
tools: Listing<Tools>?

class JsonSchema {
  /// The name of the schema.
  name: String

  /// Additional description of the schema for the LLM.
  description: String?

  /// The JSON schema for the LLM to use when generating the output.
  schema: String
}

class SchemaRegistry {
  /// The base URL of the schema registry service.
  url: String

  /// The prefix of the name for this schema, the schema ID is used as a suffix.
  ///
  /// Default: `"schema_registry_id_"`
  name_prefix: String?

  /// The subject name to fetch the schema for.
  subject: String

  /// The refresh rate for getting the latest schema. If not specified the schema does not
  /// refresh.
  refresh_interval: String?

  /// Custom TLS settings can be used to override system defaults.
  tls: SchemaRegistryTls?

  /// Allows you to specify open authentication via OAuth version 1.
  oauth: SchemaRegistryOauth?

  /// Allows you to specify basic authentication.
  basic_auth: SchemaRegistryBasicAuth?

  /// BETA: Allows you to specify JWT authentication.
  jwt: SchemaRegistryJwt?
}

class SchemaRegistryTls {
  /// Whether to skip server side certificate verification.
  ///
  /// Default: `false`
  skip_cert_verify: Boolean?

  /// Whether to allow the remote server to repeatedly request renegotiation. Enable this
  /// option if you're seeing the error message `local error: tls: no renegotiation`.
  ///
  /// Default: `false`
  enable_renegotiation: Boolean?

  /// An optional root certificate authority to use. This is a string, representing a
  /// certificate chain from the parent trusted root certificate, to possible intermediate
  /// signing certificates, to the host certificate.
  ///
  /// This field holds a secret.
  root_cas: String?

  /// An optional path of a root certificate authority file to use. This is a file, often
  /// with a .pem extension, containing a certificate chain from the parent trusted root
  /// certificate, to possible intermediate signing certificates, to the host certificate.
  root_cas_file: String?

  /// A list of client certificates to use. For each certificate either the fields `cert`
  /// and `key`, or `cert_file` and `key_file` should be specified, but not both.
  client_certs: Listing<SchemaRegistryTlsClientCerts>?
}

class SchemaRegistryTlsClientCerts {
  /// A plain text certificate to use.
  cert: String?

  /// A plain text certificate key to use.
  ///
  /// This field holds a secret.
  key: String?

  /// The path of a certificate to use.
  cert_file: String?

  /// The path of a certificate key to use.
  key_file: String?

  /// A plain text password for when the private key is password encrypted in PKCS#1 or
  /// PKCS#8 format. The obsolete `pbeWithMD5AndDES-CBC` algorithm is not supported for
  /// the PKCS#8 format.
  ///
  /// Because the obsolete pbeWithMD5AndDES-CBC algorithm does not authenticate the
  /// ciphertext, it is vulnerable to padding oracle attacks that can let an attacker
  /// recover the plaintext.
  ///
  /// This field holds a secret.
  password: String?
}

class SchemaRegistryOauth {
  /// Whether to use OAuth version 1 in requests.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// A value used to identify the client to the service provider.
  consumer_key: String?

  /// A secret used to establish ownership of the consumer key.
  ///
  /// This field holds a secret.
  consumer_secret: String?

  /// A value used to gain access to the protected resources on behalf of the user.
  access_token: String?

  /// A secret provided in order to establish ownership of a given access token.
  ///
  /// This field holds a secret.
  access_token_secret: String?
}

class SchemaRegistryBasicAuth {
  /// Whether to use basic authentication in requests.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// A username to authenticate as.
  username: String?

  /// A password to authenticate with.
  ///
  /// This field holds a secret.
  password: String?
}

class SchemaRegistryJwt {
  /// Whether to use JWT authentication in requests.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// A file with the PEM encoded via PKCS1 or PKCS8 as private key.
  private_key_file: String?

  /// A method used to sign the token such as RS256, RS384, RS512 or EdDSA.
  signing_method: String?

  /// A value used to identify the claims that issued the JWT.
  claims: Mapping<String, Any>?

  /// Add optional key/value headers to the JWT.
  headers: Mapping<String, Any>?
}

class Tools {
  /// The name of this tool.
  name: String

  /// A description of this tool, the LLM uses this to decide if the tool should be used.
  description: String

  /// The parameters the LLM needs to provide to invoke this tool.
  parameters: ToolsParameters?

  /// The pipeline to execute when the LLM uses this tool.
  processors: Listing<Processor>?
}

class ToolsParameters {
  /// The required parameters for this pipeline.
  required: Listing<String>?

  /// The properties for the processor's input data
  properties: Mapping<String, ToolsParametersProperties>?
}

class ToolsParametersProperties {
  /// The type of this parameter.
  type: String

  /// A description of this parameter.
  description: String

  /// Specifies that this parameter is an enum and only these specific values should be
  /// used.
  enum: Listing<String>?
}
