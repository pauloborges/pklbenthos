// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Consumes files from an SFTP server.
///
/// ## Metadata
///
/// This input adds the following metadata fields to each message:
///
/// - sftp_path
/// - sftp_mod_time
///
/// You can access these metadata fields using [function
/// interpolation](https://docs.redpanda.com/redpanda-connect/configuration/interpolation#bloblang-queries).
///
/// Status: stable
module com.redpanda.connect.inputs.Sftp
extends "../Input.pkl"

import "../Scanner.pkl"

fixed plugin = "sftp"

/// The address of the server to connect to.
address: String

/// The connection timeout to use when connecting to the target server.
///
/// Default: `"30s"`
connection_timeout: String?

/// The credentials to use to log into the target server.
credentials: Credentials?

/// The maximum number of SFTP sessions.
///
/// Default: `10`
max_sftp_sessions: Int?

/// A list of paths to consume sequentially. Glob patterns are supported.
paths: Listing<String>

/// Whether messages that are rejected (nacked) at the output level should be
/// automatically replayed indefinitely, eventually resulting in back pressure if the
/// cause of the rejections is persistent. If set to `false` these messages will instead
/// be deleted. Disabling auto replays can greatly improve memory efficiency of high
/// throughput streams as the original shape of the data can be discarded immediately upon
/// consumption and mutation.
///
/// Default: `true`
auto_replay_nacks: Boolean?

/// The way in which the bytes of a data source should be converted into discrete
/// messages, codecs are useful for specifying how large files or continuous streams of
/// data might be processed in small chunks rather than loading it all in memory. It's
/// possible to consume lines using a custom delimiter with the `delim:x` codec, where x
/// is the character sequence custom delimiter. Codecs can be chained with `/`, for
/// example a gzip compressed CSV file can be consumed with the codec `gzip/csv`.
///
/// Deprecated.
codec: ("auto"|"all-bytes"|"avro-ocf:marshaler=x"|"chunker:x"|"csv"|"csv:x"|"csv-safe"|"csv-safe:x"|"delim:x"|"gzip"|"pgzip"|"lines"|"multipart"|"regex:(?m)^\\d\\d:\\d\\d:\\d\\d"|"skipbom"|"tar")?

/// Deprecated.
///
/// Default: `1000000`
max_buffer: Int?

/// The [scanner](https://docs.redpanda.com/redpanda-connect/components/scanners/about) by
/// which the stream of bytes consumed will be broken out into individual messages.
/// Scanners are useful for processing large sources of data without holding the entirety
/// of it within memory. For example, the `csv` scanner allows you to process individual
/// CSV rows without loading the entire CSV file in memory at once.
scanner: Scanner?

/// Whether to delete files from the server once they are processed.
///
/// Default: `false`
delete_on_finish: Boolean?

/// An experimental mode whereby the input will periodically scan the target paths for new
/// files and consume them, when all files are consumed the input will continue polling
/// for new files.
watcher: Watcher?

class Credentials {
  /// The username to authenticate with the SFTP server.
  username: String?

  /// The password for the specified username to connect to the SFTP server.
  ///
  /// This field holds a secret.
  password: String?

  /// The path to the SFTP server's public key file, used for host key verification.
  host_public_key_file: String?

  /// The raw contents of the SFTP server's public key, used for host key verification.
  host_public_key: String?

  /// The path to the private key file, used for authenticating the username.
  private_key_file: String?

  /// The raw contents of the private key, used for authenticating the username.
  ///
  /// This field holds a secret.
  private_key: String?

  /// Optional passphrase for decrypting the private key, if it's encrypted.
  ///
  /// This field holds a secret.
  private_key_pass: String?
}

class Watcher {
  /// Whether file watching is enabled.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// The minimum period of time since a file was last updated before attempting to
  /// consume it. Increasing this period decreases the likelihood that a file will be
  /// consumed whilst it is still being written to.
  ///
  /// Default: `"1s"`
  minimum_age: String?

  /// The interval between each attempt to scan the target paths for new files.
  ///
  /// Default: `"1s"`
  poll_interval: String?

  /// A [cache
  /// resource](https://docs.redpanda.com/redpanda-connect/components/caches/about) for
  /// storing the paths of files already consumed.
  cache: String?
}
