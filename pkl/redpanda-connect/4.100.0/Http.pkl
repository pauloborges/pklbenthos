// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Configures the service-wide HTTP server.
module com.redpanda.connect.Http

/// Whether to enable to HTTP server.
///
/// Default: `true`
enabled: Boolean?

/// The address to bind to.
///
/// Default: `"0.0.0.0:4195"`
address: String?

/// Specifies a general prefix for all endpoints, this can help isolate the service
/// endpoints when using a reverse proxy with other shared services. All endpoints will
/// still be registered at the root as well as behind the prefix, e.g. with a root_path
/// set to `/foo` the endpoint `/version` will be accessible from both `/version` and
/// `/foo/version`.
///
/// Default: `"/benthos"`
root_path: String?

/// Whether to register a few extra endpoints that can be useful for debugging performance
/// or behavioral problems.
///
/// Default: `false`
debug_endpoints: Boolean?

/// An optional certificate file for enabling TLS.
cert_file: String?

/// An optional key file for enabling TLS.
key_file: String?

/// Adds Cross-Origin Resource Sharing headers.
cors: Cors?

/// Allows you to enforce and customise basic authentication for requests to the HTTP
/// server.
basic_auth: BasicAuth?

class Cors {
  /// Whether to allow CORS requests.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// An explicit list of origins that are allowed for CORS requests.
  allowed_origins: Listing<String>?
}

class BasicAuth {
  /// Enable basic authentication
  ///
  /// Default: `false`
  enabled: Boolean?

  /// Custom realm name
  ///
  /// Default: `"restricted"`
  realm: String?

  /// Username required to authenticate.
  username: String?

  /// Hashed password required to authenticate. (base64 encoded)
  password_hash: String?

  /// Encryption algorithm used to generate `password_hash`.
  ///
  /// Default: `"sha256"`
  algorithm: String?

  /// Salt for scrypt algorithm. (base64 encoded)
  salt: String?
}
