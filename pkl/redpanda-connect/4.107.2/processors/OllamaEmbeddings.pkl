// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Generates vector embeddings from text, using the Ollama API.
///
/// This processor sends text to your chosen Ollama large language model (LLM) and creates
/// vector embeddings, using the Ollama API. Vector embeddings are long arrays of numbers
/// that represent values or objects, in this case text.
///
/// By default, the processor starts and runs a locally installed Ollama server.
/// Alternatively, to use an already running Ollama server, add your server details to the
/// `server_address` field. You can [download and install Ollama from the Ollama
/// website](https://ollama.com/download).
///
/// For more information, see the [Ollama
/// documentation](https://github.com/ollama/ollama/tree/main/docs).
///
/// Status: stable
module com.redpanda.connect.processors.OllamaEmbeddings
extends "../Processor.pkl"

fixed plugin = "ollama_embeddings"

/// The name of the Ollama LLM to use. For a full list of models, see the [Ollama
/// website](https://ollama.com/models).
model: String

/// The text you want to create vector embeddings for. By default, the processor submits
/// the entire payload as a string.
text: String?

/// Options for the model runner that are used when the model is first loaded into memory.
runner: Runner?

/// The address of the Ollama server to use. Leave the field blank and the processor
/// starts and runs a local Ollama server or specify the address of your own local or
/// remote server.
server_address: String?

/// If `server_address` is not set - the directory to download the ollama binary and use
/// as a model cache.
cache_directory: String?

/// If `server_address` is not set - the URL to download the ollama binary from. Defaults
/// to the official Ollama GitHub release for this platform.
download_url: String?

class Runner {
  /// Sets the size of the context window used to generate the next token. Using a larger
  /// context window uses more memory and takes longer to processor.
  context_size: Int?

  /// The maximum number of requests to process in parallel.
  batch_size: Int?

  /// This option allows offloading some layers to the GPU for computation. This generally
  /// results in increased performance. By default, the runtime decides the number of
  /// layers dynamically.
  gpu_layers: Int?

  /// Set the number of threads to use during generation. For optimal performance, it is
  /// recommended to set this value to the number of physical CPU cores your system has.
  /// By default, the runtime decides the optimal number of threads.
  threads: Int?

  /// Map the model into memory. This is only support on unix systems and allows loading
  /// only the necessary parts of the model as needed.
  use_mmap: Boolean?
}
