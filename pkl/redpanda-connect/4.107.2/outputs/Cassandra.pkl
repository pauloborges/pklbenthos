// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Runs a query against a Cassandra database for each message in order to insert data.
///
/// Query arguments can be set using a bloblang array for the fields using the
/// `args_mapping` field.
///
/// When populating timestamp columns the value must either be a string in ISO 8601 format
/// (2006-01-02T15:04:05Z07:00), or an integer representing unix time in seconds.
///
/// ## Performance
///
/// This output benefits from sending multiple messages in flight in parallel for improved
/// performance. You can tune the max number of in flight messages (or message batches)
/// with the field `max_in_flight`.
///
/// This output benefits from sending messages as a batch for improved performance.
/// Batches can be formed at both the input and output level. You can find out more [in
/// this doc](https://docs.redpanda.com/redpanda-connect/configuration/batching).
///
/// Status: stable
module com.redpanda.connect.outputs.Cassandra
extends "../Output.pkl"

import "../Processor.pkl"

fixed plugin = "cassandra"

/// A list of Cassandra nodes to connect to. Multiple comma separated addresses can be
/// specified on a single line.
addresses: Listing<String>

/// Custom TLS settings can be used to override system defaults.
tls: Tls?

/// Optional configuration of Cassandra authentication parameters.
password_authenticator: PasswordAuthenticator?

/// If enabled the driver will not attempt to get host info from the system.peers table.
/// This can speed up queries but will mean that data_centre, rack and token information
/// will not be available.
///
/// Default: `false`
disable_initial_host_lookup: Boolean?

/// The maximum number of retries before giving up on a request.
///
/// Default: `3`
max_retries: Int?

/// Control time intervals between retry attempts.
backoff: Backoff?

/// The client connection timeout.
///
/// Default: `"600ms"`
timeout: String?

/// Optional host selection policy configurations. Highly recommended in deployments with
/// multiple DCs. Host selection is always token aware if the token can be calculated from
/// query. By default the underlying policy is round robin over all nodes. Users can
/// specify a local DC and rack to use for the DC Aware & Rack Aware policies.
host_selection_policy: HostSelectionPolicy?

/// Attempts to reconnect known DOWN nodes in every ReconnectInterval.
///
/// Default: `"60s"`
reconnect_interval: String?

/// Optional exponential reconnection policy, this replaces the default constant policy of
/// the driver.
exponential_reconnection: ExponentialReconnection?

/// A query to execute for each message.
query: String

/// A [Bloblang mapping](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about)
/// that can be used to provide arguments to Cassandra queries. The result of the query
/// must be an array containing a matching number of elements to the query arguments.
args_mapping: String?

/// The consistency level to use.
///
/// Default: `"QUORUM"`
consistency: ("ANY"|"ONE"|"TWO"|"THREE"|"QUORUM"|"ALL"|"LOCAL_QUORUM"|"EACH_QUORUM"|"LOCAL_ONE")?

/// If enabled the driver will perform a logged batch. Disabling this prompts unlogged
/// batches to be used instead, which are less efficient but necessary for alternative
/// storages that do not support logged batches.
///
/// Default: `true`
logged_batch: Boolean?

/// The maximum number of messages to have in flight at a given time. Increase this to
/// improve throughput.
///
/// Default: `64`
max_in_flight: Int?

/// Allows you to configure a [batching
/// policy](https://docs.redpanda.com/redpanda-connect/configuration/batching).
batching: Batching?

class Tls {
  /// Whether custom TLS settings are enabled.
  ///
  /// Default: `false`
  enabled: Boolean?

  /// Whether to skip server side certificate verification.
  ///
  /// Default: `false`
  skip_cert_verify: Boolean?

  /// Whether to allow the remote server to repeatedly request renegotiation. Enable this
  /// option if you're seeing the error message `local error: tls: no renegotiation`.
  ///
  /// Default: `false`
  enable_renegotiation: Boolean?

  /// An optional root certificate authority to use. This is a string, representing a
  /// certificate chain from the parent trusted root certificate, to possible intermediate
  /// signing certificates, to the host certificate.
  ///
  /// This field holds a secret.
  root_cas: String?

  /// An optional path of a root certificate authority file to use. This is a file, often
  /// with a .pem extension, containing a certificate chain from the parent trusted root
  /// certificate, to possible intermediate signing certificates, to the host certificate.
  root_cas_file: String?

  /// A list of client certificates to use. For each certificate either the fields `cert`
  /// and `key`, or `cert_file` and `key_file` should be specified, but not both.
  client_certs: Listing<TlsClientCerts>?
}

class TlsClientCerts {
  /// A plain text certificate to use.
  cert: String?

  /// A plain text certificate key to use.
  ///
  /// This field holds a secret.
  key: String?

  /// The path of a certificate to use.
  cert_file: String?

  /// The path of a certificate key to use.
  key_file: String?

  /// A plain text password for when the private key is password encrypted in PKCS#1 or
  /// PKCS#8 format. The obsolete `pbeWithMD5AndDES-CBC` algorithm is not supported for
  /// the PKCS#8 format.
  ///
  /// Because the obsolete pbeWithMD5AndDES-CBC algorithm does not authenticate the
  /// ciphertext, it is vulnerable to padding oracle attacks that can let an attacker
  /// recover the plaintext.
  ///
  /// This field holds a secret.
  password: String?
}

class PasswordAuthenticator {
  /// Whether to use password authentication
  ///
  /// Default: `false`
  enabled: Boolean?

  /// The username to authenticate as.
  username: String?

  /// The password to authenticate with.
  ///
  /// This field holds a secret.
  password: String?
}

class Backoff {
  /// The initial period to wait between retry attempts.
  ///
  /// Default: `"1s"`
  initial_interval: String?

  /// The maximum period to wait between retry attempts.
  ///
  /// Default: `"5s"`
  max_interval: String?
}

class HostSelectionPolicy {
  /// The local DC to use, enables DC aware policy.
  local_dc: String?

  /// The local rack to use, requires local_dc to be set, enables rack aware policy.
  local_rack: String?
}

class ExponentialReconnection {
  /// The maximum number of retry attempts.
  max_retries: Int

  /// The initial period to wait between retry attempts.
  initial_interval: String

  /// The maximum period to wait between retry attempts.
  max_interval: String
}

class Batching {
  /// A number of messages at which the batch should be flushed. If `0` disables count
  /// based batching.
  ///
  /// Default: `0`
  count: Int?

  /// An amount of bytes at which the batch should be flushed. If `0` disables size based
  /// batching.
  ///
  /// Default: `0`
  byte_size: Int?

  /// A period in which an incomplete batch should be flushed regardless of its size.
  period: String?

  /// A [Bloblang query](https://docs.redpanda.com/redpanda-connect/guides/bloblang/about)
  /// that should return a boolean value indicating whether a message should end a batch.
  check: String?

  /// A list of
  /// [processors](https://docs.redpanda.com/redpanda-connect/components/processors/about)
  /// to apply to a batch as it is flushed. This allows you to aggregate and archive the
  /// batch however you see fit. Please note that all resulting messages are flushed as a
  /// single batch, therefore splitting the batch into smaller batches using these
  /// processors is a no-op.
  processors: Listing<Processor>?
}
