// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Generates vector embeddings to represent input text, using the Cohere API.
///
/// This processor sends text strings to the Cohere API, which generates vector
/// embeddings. By default, the processor submits the entire payload of each message as a
/// string, unless you use the `text_mapping` configuration field to customize it.
///
/// To learn more about vector embeddings, see the [Cohere API
/// documentation](https://docs.cohere.com/docs/embeddings).
///
/// Status: stable
module com.redpanda.connect.processors.CohereEmbeddings
extends "../Processor.pkl"

fixed plugin = "cohere_embeddings"

/// The base URL to use for API requests.
///
/// Default: `"https://api.cohere.com"`
base_url: String?

/// The API key for the Cohere API.
///
/// This field holds a secret.
api_key: String

/// The name of the Cohere model to use.
model: String

/// The text you want to generate a vector embedding for. By default, the processor
/// submits the entire payload as a string.
text_mapping: String?

/// Specifies the type of input passed to the model.
///
/// Default: `"search_document"`
input_type: ("classification"|"clustering"|"search_document"|"search_query")?

/// The number of dimensions of the output embedding. This is only available for embed-v4
/// and newer models. Possible values are 256, 512, 1024, and 1536.
dimensions: Int?
