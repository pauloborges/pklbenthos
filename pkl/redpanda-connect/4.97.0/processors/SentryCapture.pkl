// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Captures log events from messages and submits them to [Sentry](https://sentry.io/).
///
/// Status: stable
module com.redpanda.connect.processors.SentryCapture
extends "../Processor.pkl"

fixed plugin = "sentry_capture"

/// The DSN address to send sentry events to. If left empty, then SENTRY_DSN is used.
dsn: String?

/// A message to set on the sentry event
message: String

/// A mapping that must evaluate to an object-of-objects or `deleted()`. If this mapping
/// produces a value, then it is set on a sentry event as additional context.
context: String?

/// A mapping that must evaluate to an object. If this mapping produces a value, then it
/// is set on a sentry event as extras.
extras: String?

/// Sets key/value string tags on an event. Unlike context, these are indexed and
/// searchable on Sentry but have length limitations.
tags: Mapping<String, String>?

/// The environment to be sent with events. If left empty, then SENTRY_ENVIRONMENT is
/// used.
environment: String?

/// The version of the code deployed to an environment. If left empty, then the Sentry
/// client will attempt to detect the release from the environment.
release: String?

/// Sets the level on sentry events similar to logging levels.
///
/// Default: `"INFO"`
level: ("DEBUG"|"INFO"|"WARN"|"ERROR"|"FATAL")?

/// Determines how events are sent. A sync transport will block when sending each event
/// until a response is received from the Sentry server. The recommended async transport
/// will enqueue events in a buffer and send them in the background.
///
/// Default: `"async"`
transport_mode: ("async"|"sync")?

/// The duration to wait when closing the processor to flush any remaining enqueued
/// events.
///
/// Default: `"5s"`
flush_timeout: String?

/// The rate at which events are sent to the server. A value of 0 disables capturing
/// sentry events entirely. A value of 1 results in sending all events to Sentry. Any
/// value in between results sending some percentage of events.
///
/// Default: `1.0`
sampling_rate: Float?
