// This file was auto-generated by pklbenthos. Do not edit it by hand.

/// Generates responses to messages in a chat conversation, using the Ollama API.
///
/// This processor sends prompts to your chosen Ollama large language model (LLM) and
/// generates text from the responses, using the Ollama API.
///
/// By default, the processor starts and runs a locally installed Ollama server.
/// Alternatively, to use an already running Ollama server, add your server details to the
/// `server_address` field. You can [download and install Ollama from the Ollama
/// website](https://ollama.com/download).
///
/// For more information, see the [Ollama
/// documentation](https://github.com/ollama/ollama/tree/main/docs).
///
/// Status: stable
module com.redpanda.connect.processors.OllamaChat
extends "../Processor.pkl"

import "../Processor.pkl"

fixed plugin = "ollama_chat"

/// The name of the Ollama LLM to use. For a full list of models, see the [Ollama
/// website](https://ollama.com/models).
model: String

/// The prompt you want to generate a response for. By default, the processor submits the
/// entire payload as a string.
prompt: String?

/// The system prompt to submit to the Ollama LLM.
system_prompt: String?

/// The image to submit along with the prompt to the model. The result should be a byte
/// array.
image: String?

/// The format of the response that the Ollama model generates. If specifying JSON output,
/// then the `prompt` should specify that the output should be in JSON as well.
///
/// Default: `"text"`
response_format: ("text"|"json")?

/// The maximum number of tokens to predict and output. Limiting the amount of output
/// means that requests are processed faster and have a fixed limit on the cost.
max_tokens: Int?

/// The temperature of the model. Increasing the temperature makes the model answer more
/// creatively.
temperature: Int?

/// Specify the number of tokens from the initial prompt to retain when the model resets
/// its internal context. By default, this value is set to `4`. Use `-1` to retain all
/// tokens from the initial prompt.
num_keep: Int?

/// Sets the random number seed to use for generation. Setting this to a specific number
/// will make the model generate the same text for the same prompt.
seed: Int?

/// Reduces the probability of generating nonsense. A higher value, for example `100`,
/// will give more diverse answers. A lower value, for example `10`, will be more
/// conservative.
top_k: Int?

/// Works together with `top-k`. A higher value, for example 0.95, will lead to more
/// diverse text. A lower value, for example 0.5, will generate more focused and
/// conservative text.
top_p: Float?

/// Sets how strongly to penalize repetitions. A higher value, for example 1.5, will
/// penalize repetitions more strongly. A lower value, for example 0.9, will be more
/// lenient.
repeat_penalty: Float?

/// Positive values penalize new tokens if they have appeared in the text so far. This
/// increases the model's likelihood to talk about new topics.
presence_penalty: Float?

/// Positive values penalize new tokens based on the frequency of their appearance in the
/// text so far. This decreases the model's likelihood to repeat the same line verbatim.
frequency_penalty: Float?

/// Sets the stop sequences to use. When this pattern is encountered the LLM stops
/// generating text and returns the final response.
stop: Listing<String>?

/// If enabled the prompt is saved as @prompt metadata on the output message. If
/// system_prompt is used it's also saved as @system_prompt
///
/// Default: `false`
save_prompt_metadata: Boolean?

/// Historical messages to include in the chat request. The result of the bloblang query
/// should be an array of objects of the form of [{"role": "", "content":""}].
history: String?

/// The maximum number of sequential tool calls.
///
/// Default: `3`
max_tool_calls: Int?

/// The tools to allow the LLM to invoke. This allows building subpipelines that the LLM
/// can choose to invoke to execute agentic-like actions.
tools: Listing<Tools>?

/// Options for the model runner that are used when the model is first loaded into memory.
runner: Runner?

/// The address of the Ollama server to use. Leave the field blank and the processor
/// starts and runs a local Ollama server or specify the address of your own local or
/// remote server.
server_address: String?

/// If `server_address` is not set - the directory to download the ollama binary and use
/// as a model cache.
cache_directory: String?

/// If `server_address` is not set - the URL to download the ollama binary from. Defaults
/// to the official Ollama GitHub release for this platform.
download_url: String?

class Tools {
  /// The name of this tool.
  name: String

  /// A description of this tool, the LLM uses this to decide if the tool should be used.
  description: String

  /// The parameters the LLM needs to provide to invoke this tool.
  parameters: ToolsParameters?

  /// The pipeline to execute when the LLM uses this tool.
  processors: Listing<Processor>?
}

class ToolsParameters {
  /// The required parameters for this pipeline.
  required: Listing<String>?

  /// The properties for the processor's input data
  properties: Mapping<String, ToolsParametersProperties>?
}

class ToolsParametersProperties {
  /// The type of this parameter.
  type: String

  /// A description of this parameter.
  description: String

  /// Specifies that this parameter is an enum and only these specific values should be
  /// used.
  enum: Listing<String>?
}

class Runner {
  /// Sets the size of the context window used to generate the next token. Using a larger
  /// context window uses more memory and takes longer to processor.
  context_size: Int?

  /// The maximum number of requests to process in parallel.
  batch_size: Int?

  /// This option allows offloading some layers to the GPU for computation. This generally
  /// results in increased performance. By default, the runtime decides the number of
  /// layers dynamically.
  gpu_layers: Int?

  /// Set the number of threads to use during generation. For optimal performance, it is
  /// recommended to set this value to the number of physical CPU cores your system has.
  /// By default, the runtime decides the optimal number of threads.
  threads: Int?

  /// Map the model into memory. This is only support on unix systems and allows loading
  /// only the necessary parts of the model as needed.
  use_mmap: Boolean?
}
